---
description: Generate commit messages from the full set of outstanding changes and update project documents at the end of a work session. Reviews every uncommitted change — not just the current session's — clusters them into commits, reconstructs log entries for sessions that never logged, and renames all affected sessions. Runnable from any agent session. Triggers on "commit", "/commit", "generate commit message", "wrap up this session". Honors manifest.json#workflow.commit_mode — "message-only" presents messages for lazygit; "agent-commits" stages and commits after confirmation.
---

<!-- GENERATED by harness sync — do not edit. Edit .agent/skills/commit/SKILL.md instead. -->
Invoke the `commit` skill: read `.agent/skills/commit/SKILL.md` and follow it exactly,
starting with its Preflight. Arguments: $ARGUMENTS
