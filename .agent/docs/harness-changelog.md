# Harness Changelog

- 2026-09-02 `templates/marketing-site/{convex/seed.ts,src/app/layout.tsx,src/vexcms/blocks/{Hero,FAQ,HowItWorks,Header,Footer,CTA,CodeShowcase}/*,public/**}`, `templates/base-nextjs/{src/components/component-example.tsx,public/favicons/favicon.ico}`: synced the www touch-ups into both templates — real favicon assets replacing Next.js's default `.ico`, `@latest`→`@alpha` in every install string, `/docs`→`https://docs.vexcms.dev`, the `CodeShowcase` keyed-`Fragment` fix, and the collection→schema code showcase. `AdminDemoButton` and the header's unconditional admin button were deliberately NOT synced (base's matrix denies `user` `adminPanel.access`, so the anonymous-demo flow would land a scaffolded project's visitor on `/unauthorized`, and granting it would expose every user's panel); the template keeps its `useCanAccessAdminPanel()` gate plus an `Admin` nav item. Both `scaffold-smoke.mjs` runs exit 0; new assets confirmed in `npm pack`. Folded into the already-versioned `create-vexcms@0.1.0-alpha.10` CHANGELOG rather than a new changeset — trigger: "we dont need a new changeset, just work it into alpha.10"
- 2026-09-02 preferences.md: added P-021 (`convex/seed.ts` is the single source of truth for site content; ship content changes by wiping the affected rows and re-running `seed:init` from the Convex dashboard, never by hand-editing documents — `init` is insert-only-if-absent, so an un-wiped row silently keeps stale content; `pages`/`headers`/`footers` are safe to wipe together, `themes` is not without also wiping the `siteSettings` singleton) — trigger: "i can just wipe the pages and reseed them from the convex dashboard, which i would prefer to do going forward"
- 2026-08-12 preferences.md: added P-004 (client evaluates `hasPermission` directly; `access` via client-bundle import to `VexAccessProvider`, `user`/`organization` via context; server guards enforce, client is advisory) and P-005 (RSC-serialization vs. bundler-import are different boundaries; only serialization strips) — trigger: RBAC spec Decisions 24 & 12 clarification, client-side permission architecture settled during spec 2026-08-12-rbac-access-control.
- 2026-08-08 `.agent/docs/specs/`, `.agent/docs/design/`: migrated specs 35 & 36 from `.pi/agent-docs/specs/` to `.agent/docs/specs/`, design assets from `.pi/design/` to `.agent/docs/design/`, updated all live references in `.agent/` — trigger: "remove the .pi folder completely and only have .omp and .agent"
- 2026-01-23 dev-processes.md: Reinforced existing rule P-005 (line 51) — agents never open browser windows. All manual browser testing delegated to developer.
- 2026-09-04 `scripts/sync-template-versions.mjs`, `.agent/docs/standards/tooling/release-and-versioning.md`, `.agent/docs/product/dev-processes.md`: expanded `pnpm version:packages` to sync `apps/www/package.json#version` to core package version and rewrite any non-`workspace:*` `@vexcms/*` pins to `~<version>`. www now rides the release train (always ships against latest packages); manifest version tracks package version instead of drifting. Guard on www path is warn-and-skip (not fatal like templates, since www is private/unpublished and version is cosmetic); templates guard remains fatal (they ship inside create-vexcms). Extracted www sync into `syncWww()` function. Dropped stale note about hardcoded admin-next/ui/richtext names (package list derived from workspace since WP-0). Documented in release standards and both dev-processes tables (command + flow step 4). — trigger: "it will always use the latest version"

- 2026-09-14 — `packages/create-vexcms/templates/marketing-site/src/vexcms/blocks/Roadmap/index.tsx` — mirrored the `<ScrollBar orientation="horizontal" />` affordance added to `apps/www` in `2026-09-13-discord-post-readiness` Step 4 (roadmap tab strip scrolls at 375/390 but showed no scrollbar). Verified identical to source; both `scaffold-smoke.mjs` runs green. Trigger: "all fixes here should be mirrored back into templates".

- 2026-09-14 — `.agent/skills/commit/references/commit-checklist.md` — added two "Must be current" gate items: public-facing surfaces (docs, READMEs, www copy) must be consistent with committed code; `apps/www`/`apps/test` changes must be considered for `template-sync` with content-vs-structure classification. Trigger: "the commit skill for this project must also include updating anything in the docs and front facing www apps to be made consistent with any changes in the files being committed".

- 2026-09-14 — `packages/create-vexcms/templates/marketing-site/convex/seed.ts` — mirrored the structural half of the developer's `apps/www` copy pass: 10 string-literal `blockType` values replaced with the `BLOCK_SLUG_*` constants the file already imported; import extended with HEADER/FOOTER/HERO. VexCMS marketing copy and page-composition changes deliberately NOT mirrored (template ships generic starter content). 27 older CHANGED files reported by template-diff predate this run and remain unsynced. Verified: both scaffold-smoke runs green; verify-scaffold typecheck+build green. Trigger: "sync first".

- 2026-09-14 — repo cleanup: deleted `.pi/` (199), `.rebuild/` (232), `.claude/` (9 tracked + 131 MB untracked), `.mcp.json`, `.playwright-mcp/`, root `tailwind.css`, and 11 rebuild-era / one-shot scripts. `claude` removed from `manifest.json#platforms.active`; `harness sync` deleted its 19 bridge files. Migrated with history: 6 planning docs + `core-migrations` → `.agent/docs/research/legacy-planning/` (with provenance README), `core-convex` + `core-valueTypes` → `specs/2026-08-23-versioning-drafts/reference/` (research.md repointed), `CUSTOM-COMPONENTS-SYSTEM.md` → `research/custom-field-components-hkt-design.md`; Claude memory note on type/schema generator divergence → `backlog.md`. `roadmap.md` source pointers and `naming-conventions.md` script examples updated. Trigger: "remove all of the outdated project documents from the various harnesses i used to use".

- 2026-09-14 — `packages/create-vexcms/templates/base-nextjs/src/app/(frontend)/@auth/(...)auth/[pathname]/view.tsx` — mirrored two `apps/www` fixes to the intercepted auth modal: dropped the in-flow `<main>` wrapper around the portaled `Dialog` (Next's post-navigation scroll handler targeted that empty element after the footer and scrolled the page to the bottom behind the modal on every client navigation into `/auth/*`), and `onOpenChange` → `router.back()` so dismissing the dialog actually closes the intercepted route. Verbatim copy per the spec's translation table; both `scaffold-smoke.mjs` runs green. `SessionButton`/header changes deliberately NOT synced (depend on the unsynced `AdminDemoButton` header drift, see 2026-09-02). Trigger: "sync these fixes back to the templates".
