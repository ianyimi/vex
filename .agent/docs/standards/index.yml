# GENERATED by harness index rebuild
root:
  anti-patterns: { description: "Corrected agent mistakes. Append-only: `- AP-NNN (YYYY-MM-DD, seen Nx) rule`. Never compact.", lines: 13 }
  directory-structure: { description: "GENERATED by `harness struct`. Do not edit manually.", lines: 20014 }
  naming-conventions: { description: "(first paragraph missing — add one)", lines: 114 }
  preferences: { description: "Style and API patterns the developer has ratified. Append `- P-NNN (YYYY-MM-DD) rule`;", lines: 13 }
backend:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  auth-adapters: { description: "and converts each table into a Vex `CollectionConfig`; non-user tables get", lines: 18 }
  convex-functions: { description: "functions with validators and return registered query/mutation objects", lines: 27 }
  file-storage-adapters: { description: "(`packages/file-storage-convex/src/adapter/index.ts:13-80`). Every method takes Convex", lines: 18 }
  schema-generation: { description: "— DO NOT EDIT`). Regenerate with `vex dev` / `vex generate`; never hand-edit.", lines: 21 }
core:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  adding-a-field-type: { description: "Verified against `packages/core/src/fields/upload/` (newest field). Follow in order.", lines: 38 }
  field-config-conventions: { description: "props) and `<Type>Field` (post-defaults, props always present: `label`, `required`,", lines: 25 }
  hkt-adapter-architecture: { description: "Core stays framework-agnostic (no react imports, no JSX) via Higher-Kinded Type slots.", lines: 25 }
  type-generation-strategy: { description: "Two complementary mechanisms; pick by WHERE the referenced data lives — mechanical rule,", lines: 29 }
docs:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  docs-site-conventions: { description: "(`.mdx`/`.md`), Fields/ (autogenerated from the fields content dir), and the API", lines: 33 }
  jsdoc-conventions: { description: "Every exported symbol needs JSDoc (eslint-plugin-jsdoc, `eslint.config.mjs:40-135`).", lines: 56 }
frontend:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  components-and-shadcn: { description: "`packages/react/src/components/ui/` via `npx shadcn@latest add <component>`. Check", lines: 22 }
  css-and-tokens: { description: "`apps/www/src/app/globals.css` (`:root` + `.dark` scopes, OKLCH color space).", lines: 23 }
  data-fetching: { description: "spread into `useQuery` — returns a live WebSocket subscription", lines: 22 }
  forms: { description: "(`packages/react/src/components/form/AppForm.tsx`); field inputs are created with", lines: 19 }
  tables: { description: "`DataTable` via `flexRender()` (`packages/react/src/components/ui/data-table/DataTable.tsx`).", lines: 20 }
testing:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  convex-integration-testing: { description: "(`src/api/test/convex/`); define an EXPLICIT modules map — convex-test cannot use", lines: 17 }
  field-type-testing: { description: "Every field type gets a PAIR of colocated tests:", lines: 18 }
  test-structure: { description: "`.spec` suffix. Vitest `globals: true` everywhere (file-storage-convex is the minimal", lines: 20 }
tooling:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  cli-architecture: { description: "for the Convex deploy, exits non-zero on failure (`dev.ts:35-47`); default mode runs the", lines: 21 }
  release-and-versioning: { description: "changesets group (`.changeset/config.json`); `@vexcms/tsconfig` and `www` are ignored.", lines: 19 }
  tsup-and-exports: { description: "`\"use client\"` components uses an ARRAY of tsup configs — server entries with NO banner", lines: 21 }
  workspace-packaging: { description: "through `pnpm-workspace.yaml#catalog`. Literal versions in package.json are forbidden.", lines: 24 }
