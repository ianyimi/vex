# v0.1.0 Launch Plan

> Ordered, dependency-checked spec sequence from `0.1.0-alpha.16` to `0.1.0`.
> Every claim below was verified against package source on 2026-09-12 — file:line
> citations are the evidence, not illustration. Supersedes the M1–M8 framing in
> `roadmap.md` for the launch track specifically.
>
> **Progress update, 2026-09-15:** `2026-09-13-discord-post-readiness` completed
> all 8 steps (`.implement-state.json`, all `pass`) — its pulled-forward slice of
> A, B1, and J is done, not merely started; see each section below for what that
> closes. Separately, `ed502a9` (`feat(core)!: split vex.config into client-safe
> and server halves`, ADR-013) shipped the config-split spec that hard ordering
> constraint 3 names as E's prerequisite — E can now take the `livePreview.url`
> function form directly instead of falling back to the template. Neither F, C,
> D, G, H, or I has started.
>
> **C re-scoped, 2026-09-20:** `2026-08-23-versioning-drafts` (56 tasks, draft) is
> superseded by `2026-09-20-versioning-drafts` (18 task groups) — see that spec's
> `spec-tasks.md` header for the full "what changed" record. The two-row model, RBAC
> shape, and read-path analysis from the original `design-review.md` carry over
> unchanged; the write paths now call F's shipped pipeline instead of building their
> own, and the status-filter integration targets the current
> `access-constraint-builder` API rather than the deleted one the original draft
> assumed. Four decisions were made during re-scoping: proceed without B2 (whole-
> document snapshots), `publish` validates strictly, version history ships unbounded
> with no automatic pruning, and list-view pair-collapsing ships in this spec rather
> than deferring to I. Implementation has not started.

**Goal of 0.1.0:** good enough that a stranger can build a real test project on it
without hitting a wall we already know about, and without reading a caveat to
understand why something advertised does nothing.

**Status key:** ☐ not started · ◐ in progress · ☑ done

---

## Sequence

Spec IDs are stable labels, not positions — reorder freely without renaming them.
The bodies below follow execution order, so you can read top to bottom.

> **`2026-09-13-discord-post-readiness` runs before all of this** and is not part of
> the track. It pulled forward everything a Convex Discord visitor could reach: A's
> bulk-selection bar, A's two README items, B1 in full, and the admin + `www`
> halves of J at 375/390/768/1024. **Confirmed done** — all 8 steps recorded
> `pass` in `.implement-state.json`, 2026-09-14. A, B and J below are annotated
> where it reduced them.
>
> **The config-split spec (hard ordering constraint 3, below) also shipped** —
> `ed502a9`, ADR-013, 2026-09-15. E's soft gate is now cleared for real, not just
> "if it lands in time": `livePreview.url` can be a function from the start.
>
> **E shipped** — `b5263dc`, 2026-09-20, spec `2026-09-18-live-preview`. C is next,
> and inherits exactly one obligation from it: E's overlay sits on whatever document
> the consumer's own query returned, so C changes only that base layer (fetch the
> draft instead of the published row). Nothing in E needs revisiting to do that.

| Order | Spec | Gate it clears | Status |
|---|---|---|---|
| 1 | **A** — Data-table integrity & dead config | Nothing shipped is resolved-but-unread | ◐ discord-pulled items done; remaining scope (items 2, 3, 5, scope-split) not started |
| 2 | **B** — Public API surface | Nothing exported lies about what it does | ◐ B1 done (discord-pulled); B2 ADR not written |
| 3 | **F** — Lifecycle hooks & validation | Extensibility + server-side constraint enforcement | ◐ pipeline + `apps/www`/`apps/test`/template wiring done, 2026-09-19; the lifecycle-hooks guide and codegen-assertion scripts remain |
| 4 | **E** — Live preview | Editing feedback loop | ☑ done — `b5263dc`, 2026-09-20 (spec `2026-09-18-live-preview`) |
| 5 | **C** — Versioning & drafts | The one feature the README tells users to wait for | ◑ re-scoped, 2026-09-20 — spec `2026-09-20-versioning-drafts` (18 task groups); implementation not started |
| 6 | **D** — Richtext field | The published `richtext-plate` package becomes usable | ☐ not started |
| 7 | **G** — Field input consistency pass | No input looks half-built | ☐ not started |
| 8 | **H** — Edit-view overhaul | Editing is not hostile | ☐ not started |
| 9 | **I** — List-view overhaul | 200 rows is usable | ☐ not started |
| 10 | **J** — Responsive / mobile UI pass | Final gate before tagging | ◐ discord-pulled slice (admin + www at 375/390/768/1024) done; full 7-width regression pass not started |

### Sequencing principle

Each spec consumes what earlier specs built. **No spec writes inert code whose only
justification is a later spec.** Applying that rule rejected three things an
earlier draft of this plan called for; the reasoning is recorded because it is what
stops the order drifting back.

**1. F moves ahead of C.** The earlier draft told C to "route draft writes through
a shared write pipeline so F can insert one stage instead of five" — which is
exactly the anti-pattern: C building structure whose only payoff is F. With F
first, the pipeline already exists and C simply calls it. Four further reasons the
swap is strictly better:

- F closes a live correctness hole — `min`/`max` on every field are unenforced
  server-side today (see F's Finding 2). Every alpha consumer is affected now, and
  the behavior change wants maximum shake-out time before 0.1.0.
- F is core API. Freezing it before the two largest features consume it beats
  co-evolving them.
- C is the biggest, riskiest spec in the track (56 open tasks in
  `2026-08-23-versioning-drafts`). If it slips, F has already shipped value.
- **The mechanism drafts need from F is independently required today.** A draft
  save must be able to persist an incomplete document without tripping `required`
  — and F already needs exactly that lenient mode for ordinary partial `update`s
  (ADR-011, constraint 2). So F builds nothing speculative for C; C reuses a mode
  that exists for its own reasons.

  Verified there is no reverse retrofit: `_status`/`_version` are framework-owned
  and read-only, so they flow past validation the way `updatedAt` already does;
  and "does `afterChange` fire on a draft save?" is a semantic question C answers
  by *calling* F's dispatcher. Were C first, it would have to invent hook
  semantics with no hook system to hang them on.

**2. B2 ships a decision, not a reserved config flag.** See B2.

**3. A stops half-wiring what I rebuilds.** See A's scope split.

### Hard ordering constraints

Only these four are real. Everything else may be resequenced freely.

1. **B before F and D.** B2's localization decision determines whether validation
   runs per-locale and what a `richtext()` value's storage shape may be. It is a
   decision document, so it blocks on nothing and costs a day.
2. **F before C, D, G, H, I.** Everything downstream either calls the hook
   dispatcher or renders its validation errors. Server-only async validation is a
   different render path from instant client Zod errors; building the inputs first
   means reworking every error display.
3. **The config-split spec before E — ☑ done.** Splitting the config into
   per-runtime client/server entry points is tracked outside this track but was
   a prerequisite here: it is what lets `livePreview.url` be a function rather
   than a serializable template, by removing the `sanitizeConfigForClient`
   function stripping rather than working around it. **Shipped in `ed502a9`
   (ADR-013), 2026-09-15** — E can build directly against the function form; no
   fallback needed. It also unblocked collection-level `admin.components.preview`,
   so live preview and the relationship field remain its two consumers.
4. **J last.** It audits the finished surface. Running it earlier audits a surface
   that is about to change.

**E moved ahead of C** for the same reason, once the transport was pinned down
(ADR-012). Reading unsaved form state over `postMessage` removes the only thing
that needed drafts — verifying autosave against the preview. C then changes only
the overlay's *base layer*, which is additive. It also means the two public-API
specs (F and E) both land before the two big feature specs consume them.

A secondary payoff, and an independent argument for the design: an
autosave-to-shared-draft preview gives every editor of a document one shared
state, which is the last-write-wins problem `backlog.md`'s concurrent-edit entry
describes. Per-editor form state sidesteps it, so live preview is not blocked on
the concurrency design.

`A` before `I` is no longer a constraint — the overlap that created it has been
removed from A's scope.

## A — Data-table integrity & dead config

Dead config in the admin data table and the collection config that is supposed to
drive it. Mechanical, one reviewable diff.

> **Reduced by `2026-09-13-discord-post-readiness` — ☑ confirmed done,
> 2026-09-14.** That spec pulled forward the three items a Discord visitor could
> reach: the unreachable bulk-selection bar (`d7384b6`), the root README's
> `convex env set` gap, and its two false sortable/searchable claims. All 8 of
> that spec's steps recorded `pass`. They are struck from A's scope below rather
> than restated. What remains open in A is items 2, 3, 4, 5 and the scope split
> below — none of that has started.

### Scope

1. **`enableSorting` / `enableHiding` are dead.** `useReactTable` receives only
   `getCoreRowModel` and a `state` of `{ rowSelection }` (`DataTable.tsx:199-212`);
   headers render through `flexRender` with no click handler (`:238-247`). Yet 8
   column defs set `enableSorting: true` and 12 set `enableHiding: true`
   (`fields/*/columnDef.tsx`).
2. **The whole `admin.table` block is unconsumed.** `defaultPageSize`,
   `serverPageSize`, `pageSizeOptions`, `defaultColumns`, `defaultSort` all resolve
   with defaults at `collections/config.ts:177-192`; `CollectionListView` passes
   none of them.
3. **`defaultSort.field` defaults to `"_createdAt"`** — a field Convex does not
   have (it is `_creationTime`). The string appears exactly once in all of
   `packages/`, at `collections/config.ts:188`. Harmless only because nothing reads
   it, which is the point.

Plus, in the same pass:

4. **`updatedAt` verification (no implementation).** Already fully shipped —
   auto-injected (`collections/config.ts:150-163`), opt-out via top-level
   `timestamps: false` (`collections/types.ts:262-275`), reserved-key guards at
   compile time (`config.ts:83-90`) and runtime (`config.ts:115-128`) with a
   `meta.locked` escape hatch for better-auth, stamped after the access check
   (`api/create/server.ts:92-99`). Confirm behavior, document it, close the ask.
5. **Settle the `vex_` internal-table convention** — decided: all framework tables
   are `vex_`-prefixed (`vex_versions` first). Write the naming + migration rule
   down here, because C needs `vex_versions` and I will want a `vex_`-prefixed
   preferences table for saved views. One convention, decided once.

### Scope split — A fixes bugs, I builds the table

The earlier draft of this plan had A decide "how much of `admin.table` to wire
versus delete", with a half-day threshold for threading `order`. That is the
double-work trap: A would part-wire a surface that I then rebuilds. Split by
whether the work needs UI that does not exist yet.

**A wires what is plumbing-only** — no new UI, so no rework when I lands:

- `defaultPageSize`, `serverPageSize`, `pageSizeOptions`. `usePaginatedQuery`
  already threads a `clientPageSize` and a `query.paginationOpts`
  (`usePaginatedQuery.ts:152, 179-184`). Consuming them is wiring.

**A deletes what cannot be consumed without new UI**, and I reintroduces each one
*together with* the control that reads it:

- `defaultSort` and the 8 `enableSorting: true` column flags — sorting needs
  clickable headers and `order` threaded through the paginated query.
- `defaultColumns` and the 12 `enableHiding: true` column flags — column
  visibility needs a menu.

Deleting these is not a user-visible break: they do nothing today, so nothing
observable changes. Re-adding them in I is additive. The alternative — leaving
them resolved-but-unread for six specs while the README documents them — is the
dishonesty this spec exists to remove.

### Acceptance

- Every remaining key under `admin.table` has a consumer; the UI-dependent keys
  and their column flags are gone. Grep-provable in both directions.
- `_createdAt` appears nowhere in `packages/`.
- The `vex_` convention is written down where C and I will find it.
- `updatedAt` behavior confirmed and documented.
- 947+ tests green; `harness doctor` clean.


---

## B — Public API surface

One item remains. B1 moved out — see below.

### B1 — Defer the auto-migration stubs — MOVED, ☑ done

**Implemented by `2026-09-13-discord-post-readiness` Step 2** (recorded `pass`,
2026-09-14). Pulled forward because this exact audience reads package exports,
and `diffSchema` returning an empty diff for every input is the worst thing to
be caught shipping.

That spec un-exported the four stubs while keeping
`cli/src/lib/generateSchema.ts:131-217`'s real orchestration intact, throws on
`schema.autoMigrate: true` at config resolution, and deleted
`core/README.md`'s `### Auto-Migration` section — confirmed gone from the repo.
Nothing else in B depends on it. **B2 below has not started.**

### B2 — Localization: decide the model, ship no code

There is **zero** localization support in `core/src` today. That is fine for 0.1.0
test projects. What is not fine is deciding it *after* users have data, because
locale variants change how fields are stored and no optional config flag makes a
data migration non-breaking.

**Correction to an earlier draft of this plan.** It called for reserving
`localized?: boolean` on fields and `localization` on the root config — parsed,
stored, enforcing nothing — by analogy with `livePreview: { url }` and
`versions.drafts`. That analogy does not hold, and the reservation should not be
built:

- Adding an optional config option later is **already non-breaking**. The
  reservation buys nothing a future additive change would not.
- The genuinely breaking part is the stored shape of localized data, which an
  inert boolean does nothing to protect.
- An inert flag is worse than an absent one: a user sets `localized: true`, gets
  no localization, and reasonably reports it as broken.

So B2's deliverable is **an ADR and nothing else**:

- **Adapter interface vs built-in.** The adapter framing is plausible —
  `VexAuthAdapter` / `VexStorageAdapter` are the established extension pattern —
  but localization touches schema generation, which no existing adapter does.
  Decide, and record why the other was rejected.
- **Storage shape.** Sibling columns (`title_en`, `title_fr`), nested map
  (`title: { en, fr }`), or row-per-locale. This is the irreversible choice; the
  rest is additive.
- **Interaction with versioning.** Whether a version snapshot is per-locale or
  whole-document. C needs this answer, which is the second reason B precedes it.

**Why the decision must still land before D and F:** a `richtext()` value's
storage shape has to be compatible with the chosen model, and validation has to
know whether it runs per-locale. A decision is sufficient for both. No code.

**Acceptance:** an ADR recording the model, the storage shape, the versioning
interaction, and the rejected alternatives with reasons. Zero production diff.

---

## F — Lifecycle hooks & validation

> **Status, 2026-09-19: ◐ pipeline, `apps/www`, `apps/test`, and both templates
> shipped** (spec `2026-09-18-lifecycle-hooks-validation`, ADR-010/ADR-011). All
> 6 acceptance criteria below hold against every wired app/template.
> `createVexMutations` wiring mirrors file-for-file across `apps/www`,
> `apps/test`, and `base-nextjs` (inlined into `vex.ts` itself — the spec's
> original separate-`triggers.ts` plan was dropped during `apps/www`
> implementation); `marketing-site`'s `pages.ts` also got the `slug`
> uniqueness `textValidator` `apps/www` uses. Remaining: the codegen
> assertion that no generated mutation uses the raw builder (acceptance
> criterion 6 — not yet automated, only true by inspection today) and the
> `lifecycle-hooks.mdx` guide (step 8).

Two findings drive this spec's shape.

### Finding 1 — after-hooks cover none of validation

`convex-helpers` triggers are after-write by construction. They cannot reject a
write or transform a payload. They cover cascading deletes, denormalized counts,
webhooks, revalidation dispatch — and none of slugify, derived fields,
sanitization, uniqueness, or rejecting bad input.

### Finding 2 — constraints we already ship are not enforced server-side

`create`'s entire write path is `hasPermission` → `stampUpdatedAt` → `db.insert`
(`api/create/server.ts:62-104`). **There is no validation stage.** Yet the field
docstrings claim there is:

> `min`/`max` are runtime validation constraints, NOT schema constraints …
> Validation happens in the admin panel **and mutation handlers**
> — `fields/text/validator.ts:12-14`, identical in `fields/number/validator.ts:12-14`

The admin-panel half is true (Zod via `adminFieldToInputSchema`, `AppForm.tsx:16`).
The mutation-handler half is false — no `inputSchema`/`zod`/`validate` reference
exists in `api/create` or `api/update`.

| Enforced server-side (by Convex schema validation on write) | Not enforced anywhere server-side |
|---|---|
| types — `v.string()`, `v.number()` | `text` min/max length |
| required vs optional — `v.optional(…)` | `number` min/max value |
| `select` option membership — real literal union, `select/validator.ts:33-36` | `date` min/max timestamp |
| | `array` / `blocks` / `upload` min/max count |
| | `url` well-formedness (`v.string()` only) |

A caller using the documented Local API (`docs/guides/local-api.mdx`) can write
10,000 characters into `text({ max: { value: 100 } })`.

So the deliverable is **the stage**, not the hook. `validate()` is nearly free once
a before-write stage exists, because it is the same insertion point as
`beforeChange`.

### The pipeline

One stage in the shared write path. Transform first, validation last, because the
invariant is *whatever hits the DB satisfies the declared constraints*.

```
resolveAuth
  → hasPermission(changes = caller's raw payload)   ← unchanged
  → beforeChange(doc)        transform + derived fields
  → generated Zod            structural constraints (exists; never runs server-side)
  → validate(field)          custom per-field rules; async; receives ctx
  → stampUpdatedAt                                  ← unchanged
  → db.insert / db.patch
  → triggers: afterChange / afterDelete
```

**Two hook points, not four.** One transform stage plus one validation stage
subsumes Payload's `beforeValidate` / `afterValidate`. Do not port the full ladder.

### Implementation constraints

1. **The stage goes after `hasPermission`, mirroring `stampUpdatedAt`.**
   `api/create/server.ts:92-94` already states the rule: framework-added values go
   after the check so payload-dependent rules see exactly what the caller sent.
   Sharper reason: field-level RBAC inspects `changes`, so a `beforeChange` running
   *first* would let our own slugify hook add `slug` to `changes` and get **denied**
   for a role holding `update: () => ({ title: true })`.
2. **Partial updates need a partial validation mode.** `update` takes
   `Partial<…>` (`api/update/server.ts:37`) but `adminFieldToInputSchema` builds
   whole-document schemas — the form seeds every field. Running them against a
   patch fails `required` on every absent key. Validate only present keys; skip
   required-ness for absent ones. **This is the detail most likely to bite.**
3. **Hand hooks the merged document.** `update` already loads the stored row to
   authorize against it, so merge `stored ∪ patch` and give hooks a complete
   document — cross-field rules (`endDate > startDate`) must work on partial
   writes. No extra read.
4. **`validate()` is server-only, async, and receives `ctx`.** See ADR-011.
   Uniqueness is the most-requested CMS validation, needs a DB read, and is racy
   in most frameworks — but Convex mutations are transactional, so a `validate()`
   querying `ctx.db` inside the same mutation is genuinely sound. Generated Zod
   runs on both client and server for instant feedback; `validate()` is
   server-only because it can read the DB. Document the split so nobody expects a
   uniqueness check to fire in the form.

### Triggers integration

`convex-helpers/server/triggers` is **not a Convex component** — no
`convex.config.ts`, no `app.use()`. It is a plain library:
`new Triggers<DataModel>()` + `customMutation(rawMutation, customCtx(triggers.wrapDB))`
(source: `get-convex/convex-helpers`, `packages/convex-helpers/server/triggers.ts:57-89`).

Already a dependency of `@vexcms/core` — `convex-helpers: catalog:`, pinned
`0.1.120` in `pnpm-workspace.yaml:54`. Zero new dependencies.

- **Their loudest caveat does not apply to us.** "Forget the wrapper and triggers
  silently never run" is fatal in a hand-written app; we *generate* the mutations,
  so codegen guarantees every write uses the wrapped builder. Structural advantage
  — exploit it, and add the codegen assertion.
- **Coverage hole to document, not fix.** Triggers do not fire for Convex dashboard
  edits, `npx convex import`, or another component's direct writes. This is the
  same hole `backlog.md` records for server-side revalidation dispatch. Document
  hooks as "fire on writes through the vexcms API", never "always".
- **Veto semantics need a probe.** The maintainers state a throwing trigger
  rethrows from the `ctx.db.*` call while also describing the write as committed —
  ambiguous against Convex's normal transaction rollback. Verify empirically before
  designing any throw-to-reject behavior. Independent of the outcome, `before*`
  hooks live in the server path where rejection is unambiguous.
- **Recursion is BFS-queued**, and multiple triggers on one table can observe each
  other's intermediate denormalized state. Note it in the hook docs.

### Acceptance

- `text({ max: { value: 100 } })` rejects a 101-character write through the Local
  API, not just through the form.
- The two lying docstrings (`text/validator.ts:12-14`,
  `number/validator.ts:12-14`) become true.
- A partial `update` touching one field does not trip `required` on absent fields.
- `beforeChange` deriving `slug` from `title` is not denied by a field-map role
  that can only write `title`.
- An async `validate()` performing a `ctx.db` uniqueness query rejects a duplicate.
- `afterChange` / `afterDelete` fire through generated mutations; a codegen test
  asserts no generated mutation uses the raw builder.
- Hook coverage limits documented in a guide.

---

## E — Live preview — ☑ SHIPPED (`b5263dc`, 2026-09-20)

> **Delivered.** Spec `2026-09-18-live-preview`, all 8 task groups verified. The
> design below is kept as the record of why it is shaped the way it is; where the
> build diverged from it, the divergences are listed here and the spec's own
> `## Design Decisions` is authoritative.
>
> **Divergences from the design as written:**
> - **Transport is dual — `postMessage` AND `BroadcastChannel`.** `postMessage`
>   alone cannot reach a tab the admin never opened (duplicated, reopened from
>   history, moved to another monitor); `BroadcastChannel` covers those same-origin
>   cases with no window reference. Neither reaches another browser or device —
>   that remains out of scope.
> - **The preview gate is a session-verified cookie, not a request header.** Reading
>   a header with `next/headers` in the site layout opted every public route out of
>   static prerendering, undoing the SEO spec's work. `proxy.ts` still verifies the
>   session on `?vexLivePreview=1`, but now sets a short-lived `vex-live-preview`
>   marker cookie that `LivePreviewProvider` gates on client-side. `/` stays static.
> - **No `NextLivePreviewProvider` in `@vexcms/next`.** With no header read it had
>   no job, and the config cannot cross the RSC boundary as a prop (ADR-013 —
>   `livePreview.url` and field validators are functions). Projects mount a local
>   `"use client"` provider that imports `~/vex.config` directly, mirroring the
>   admin's existing `clientProviders.tsx`.
> - **Split-pane width persists in a cookie, not `autoSaveId`/localStorage**, read
>   server-side so the pane paints at its remembered width instead of snapping.
>
> Docs: `apps/docs/src/content/docs/guides/live-preview.mdx`.

`livePreview: { url }` already parsed and was stored on the resolved config
specifically so this was non-breaking. Nothing else existed — a grep of
`packages/*/src` for `livePreview`, `postMessage`, or `BroadcastChannel` found no
implementation.

**Ships before versioning/drafts.** Reading unsaved form state removes the only
thing that needed drafts — "verify autosave updates the preview". There is no
reverse dependency: when C lands it changes only the *base layer* the overlay sits
on (fetch the draft instead of the published document), which is additive.

### Design (ADR-012)

**Transport: `postMessage`, not `localStorage`.** `livePreview.url` is
user-configured and may be a different origin, and `localStorage` is
origin-partitioned — a cross-origin preview can never read the admin's store. A
`postMessage` transport has to exist regardless, and once it does `localStorage`
adds jank, a quota, persistence to clean up, and a documented throw hazard
(`ui/ThemeProvider.tsx:36-56`) without adding capability. Persistence across a
preview reload is handled by a **handshake** — the preview announces the document
it renders on mount, the admin replies with current values.

**Shape: overlay the consumer's own result; do not let a wrapper *be* the
mechanism.** Making a `useVexQuery` wrapper the only path was rejected — it would
force every consumer onto one fetching library, permanently commit us to
re-exposing TanStack's option surface, strand RSC `initialData` consumers, and
fail to compose at nested render sites. Instead the provider holds an
`id → unsaved values` map and a hook shadows whatever the consumer already
fetched:

```tsx
const { data: pages } = useQuery({ ...convexQuery(api.pages.getBySlug, { slug }), initialData })
const page = useVexPreview(pages?.[0])
```

That works against the template's current TanStack Query + `convexQuery` path
(`apps/www/src/app/(frontend)/(site)/PageContent.tsx:31-34`), a raw Convex
`useQuery`, or an RSC-passed `initialData`. The `id → values` map (rather than "the
one document under edit") solves nesting for free: a page rendering a
`relationship` to an author, while the author is the document being edited,
overlays correctly because each render site resolves its own document by id.

A `useVexQuery` convenience **may still ship as sugar over that primitive** —
`useQuery + useVexPreview` composed is a few lines and gives the transparent call
consumers reasonably want. Build the primitive first; add the helper if the
ergonomics warrant it.

**Matching: on `doc._id`.** The admin knows `(collection, id)`; the preview target
fetches by slug but the fetched document carries `_id`. No storage-key scheme, no
URL contract, no configuration. Two cases `_id` alone misses, both carried in the
handshake payload:

- **A document being created has no `_id`** — match on a client-generated
  temporary id in the preview URL plus the collection slug.
- **The merge is not a shallow spread.** `{ ...doc, ...values }` destroys nested
  `group` and `blocks` values when the form holds a partial subtree; it must walk
  the field config. `useLiveFieldMerge` already solves this in the opposite
  direction (server values into form state) — extract the shared logic rather than
  writing a second merge.

**No preview route — the target is the real route.** A single app-wide `/preview`
route that receives a path plus live data and renders the matching page was
rejected: Next's App Router has no `renderRoute(path, data)` primitive, so it
would need a `/preview/[...path]` catch-all re-implementing the app's own routing,
every previewable page refactored into a data-injectable component, and the
preview rendering through a different code path than production. Instead
`livePreview` resolves a document to its **real** URL; the real route picks up the
overlay. Pixel-identical to production because it is production, and no new route
to secure.

**Centralize the provider, not the route.** The appeal of one preview route was
having a single place to secure, with public routes carrying no listener. Keep
that property without the routing duplication: one `<VexLivePreviewProvider>`
mounted in the layout owns the gate, the origin check, and the transport, while
the rendered route stays the real one. When the gate is off it installs no
listener and `useVexPreview` returns the document untouched, so production pays
nothing. This is how Next's own `draftMode()` is shaped — a request-level flag,
not a parallel route tree.

**`livePreview.url` is a function, per the config-split spec.** As the config
stands, `sanitizeConfigForClient` strips every function before it reaches the
browser (`fields/relationship/preview.tsx:11-16` depends on this;
`config/sanitizeConfig.test.ts:93-102` demonstrates the nulling), which would
force a serializable interpolating template. **A separate spec splitting the
config into per-runtime client/server entry points lands before this one**, so
authored functions survive to the client and `url: (doc) => string` works. That
buys paths interpolation cannot express — locale prefixes, nested category
segments, conditional routes — and it also unblocks collection-level
`admin.components.preview`, which `resolveRelationshipPreview`'s own docstring
currently calls impossible in the browser. If the split slips, fall back to the
template; the overlay architecture is unaffected.

**Lifecycle hooks do not run on preview data.** `beforeChange` is server code with
a mutation `ctx` and is not contractually pure, so running it per keystroke could
fire webhooks or write rows — and it would need a server round trip, destroying
the local-overlay latency that is the point. Unlocking it properly needs a new
contractually-pure `derive` hook class, which would run client-side and is scope
creep on F. Document the limitation.

### Scope

- `postMessage` transport with a mount handshake, origin-checked both directions
  against a configured allowlist.
- `<VexLivePreviewProvider>` holding the `id → values` map, and `useVexPreview`.
- **Add `livePreview` to the collection admin config.** It exists only on globals
  today — `globals/types.ts:36, 63` declare `livePreview?: { url: string }`;
  `collections/types.ts`' `AdminCollectionConfigInput` declares nothing, despite
  `core/README.md:192-197` claiming "collection/global". Widen `url` to the
  function resolver form on both.
- Preview panel in the collection and global edit views; support both an embedded
  iframe and a popped-out window.
- Gate preview mode per request — see the security requirements below.
- Wire the marketing template and `apps/www` as the dogfooding consumer.

### Security — design this, do not discover it

The preview surface is the customer's **public** site. A route that
unconditionally listens for `postMessage` and renders the payload lets anyone
iframe it and inject content — spoofing now, XSS on the customer's domain once
richtext renders markup. All four are required:

1. Origin allowlist, explicitly configured rather than inferred.
2. Preview mode opt-in per request. In the default scaffold the admin is same-site
   and authenticated, so the route can verify the session cookie rather than share
   a secret.
3. Validate the payload against the field schema before rendering — the generated
   `adminFieldToInputSchema` Zod schemas exist for exactly this.
4. Richtext renders through the sanitizing renderer, never raw payload HTML.

### Acceptance

- Typing in a field updates the preview with no save and no autosave configured.
- A preview reload recovers current state via the handshake.
- A cross-origin `livePreview.url` works; a non-allowlisted origin is rejected.
- Two editors on one document each see their own unsaved changes.
- A `blocks` field re-renders on reorder and on per-block field edits.
- Preview respects RBAC — a field the role cannot read is never rendered.
- Documented limitation: the preview shows pre-hook state, so a `beforeChange`
  derived value does not appear until save.

---

## C — Versioning & drafts — ◑ RE-SCOPED, NOT IMPLEMENTED

The one true blocker. `core/README.md:121-126` tells users **not** to model publish
state as a hand-written `status` field because it becomes redundant — so today a
user either waits or builds something we told them to throw away.

**Re-scoped, 2026-09-20:** `.agent/docs/specs/2026-08-23-versioning-drafts` (56
tasks, draft) predated F and E and is superseded by
`.agent/docs/specs/2026-09-20-versioning-drafts` (18 task groups) — see that spec's
`spec-tasks.md` header for the full record of what changed and its `design-review.md`
carry-over. The four constraints below are folded into the new spec's Design
Decisions and Step list; implementation has not started.

### Constraints that must be in the design, not follow-ups

1. **`changes` must reach `hasPermission` on every draft write path.**
   Field-level RBAC denies a write by inspecting `changes` against a resolved field
   map — `create` does this at `api/create/server.ts:86-88`. `DRAFT_ACTIONS`
   (`access/constants.ts`) already types `readDrafts`/`saveDraft`/`publish`/
   `unpublish`, with **zero implementers**. If `saveDraft` omits `changes`, a role
   restricting a field on `update` gets no equivalent restriction on a draft save.
2. **Reuse the existing reserved-key machinery for `_status` / `_version`.**
   `RESERVED_COLLECTION_FIELDS` (`collections/constants.ts:40-57`) already gives
   compile-time rejection, runtime rejection, and a `meta.locked` escape hatch.
   Do not invent a second mechanism.
3. **Draft writes call F's write pipeline; they do not build one.** F ships first
   precisely so this is consumption rather than construction. Two things to reuse
   rather than reinvent:
   - the **lenient validation mode** F built for partial `update`s — a draft save
     persists an incomplete document without tripping `required`;
   - the **hook dispatcher**, so C only has to answer the semantic question of
     which hooks fire on `saveDraft` versus `publish`.
4. **`vex_versions` per the A-ratified `vex_` convention**, and per-locale versus
   whole-document snapshots per B2's ADR.

### Acceptance

- `versions.drafts: true` produces a real draft/publish workflow: reads return the
  published document by default, `readDrafts` returns drafts, `publish` promotes.
- Field-level RBAC applies identically to `update` and `saveDraft` — proven by a
  test, not by inspection.
- Version history is viewable and a prior version restorable from the edit view.
- Autosave writes drafts, never the published document.
- `core/README.md:119-126` and `:184-190` rewritten from "not shipped" to shipped.
- Live preview's base layer can fetch the draft instead of the published document
  — additive to E, which already handles the overlay.

---

## D — Richtext field

**Far smaller than the roadmap implies.** `packages/richtext-plate/src` is already
**3,219 lines of working editor** — 14 features, toolbar, table toolbar, image
upload with media-library integration, and a static renderer. The work is wiring,
not authoring.

### What is actually missing

- `richtext-plate/src/index.ts` exports **three orphan types** (`:1-3`). The
  editor and renderer are unreachable from the package root. The `./editor` and
  `./render` subpaths are declared in `package.json#exports` and do build.
- Four files carry `@ts-nocheck`: `editor/PlateEditorField.tsx`,
  `editor/plateEditor.ts`, `render/RichText.tsx`, `types.ts`.
- `PlateEditorField.tsx` imports `VexEditorComponentProps` and `RichTextDocument`
  from `@vexcms/core`. **Neither type exists** — grep of `core/src` returns
  nothing. This is why typechecking is disabled.
- No `richtext()` factory in core. The placeholder is already commented and waiting
  at `fields/constants.ts:85-89`.

### Scope

1. Add `RichTextDocument` + `VexEditorComponentProps` to core.
2. Add `richtext()` to `core/src/fields/richtext/` following the established
   per-field layout: `config.ts`, `types.ts`, `inputSchema.ts`, `validator.ts`,
   plus the two test files every other field has.
3. Register in **both** react registries — `fieldInputComponents` and
   `fieldCellComponents` (`react/src/components/fields/index.tsx:45-276`) — and add
   a `columnDef.tsx`. A cell renderer for a rich-text value needs a plain-text
   excerpt, not markup.
4. Delete all four `@ts-nocheck` pragmas and fix what they were hiding.
5. Export the editor and renderer from `richtext-plate/src/index.ts`.
6. Honor B2's localization decision in the storage shape.

**Import-cycle warning:** ADR-009's consequences apply. Field files must import
leaf modules (`../../form/createFieldInput`), never the `../../form` barrel — the
cycle is still in the module graph, only unexercised.

### Acceptance

- `richtext()` round-trips through schema gen, type gen, admin form, and cell.
- `@ts-nocheck` appears nowhere in `packages/richtext-plate`.
- A consumer typechecks against the published tarball (the WP-0 probe pattern).
- `docs/fields/coming-soon.mdx` loses `richtext`; a `docs/fields/richtext.mdx`
  replaces it.
- `core/README.md:21-24, 40-41, 81-83` corrected — they currently state three
  times that no `richtext` field exists.

---

## G — Field input consistency pass

Every field type has an input and a cell component
(`react/src/components/fields/index.tsx:45-276`), but quality is uneven.

### Scope

- **`relationship`** is the named offender: unlike the media picker it has no
  consistent picker surface, no loading states, and looks rudimentary. It has a
  `useRelationshipPickerOptions` hook to build on.
- Sweep all 13 inputs (12 + richtext from D) for: loading/pending states, empty
  states, error display, keyboard access, focus rings, disabled/read-only
  rendering, and `admin.placeholder` / `admin.width` / `admin.cellAlignment`
  honoring.
- Add the server-only async validation error path from F.
- **MIME filter in the media library grid** — `MediaLibaryGrid.tsx:36` carries a
  TODO and `upload()` already accepts `accept` (`fields/upload/config.ts:6-40`),
  so the field declares a constraint the picker ignores.
- Conditional fields (`admin.condition`) if they land here rather than in H:
  confirmed absent today; visibility is all-or-nothing via `admin.hidden`. Any real
  content model needs it (`linkType: internal | external`). A predicate evaluated
  in the form renderer; no schema impact. `useVisibleFields.ts` is the natural home.

**Acceptance:** every input renders correct empty / loading / error / read-only
states; the a11y assertions already in the field suites extended to cover them; no
field visually inconsistent with its siblings.

---

## H — Edit-view overhaul

### Scope

- **Unsaved-changes navigation guard.** Dirty state currently only disables the
  Save button (`CollectionEditView.tsx:63-73`); nothing blocks navigating away.
  Losing work on a misclick is the worst first impression per line of code saved.
- **Duplicate document** — absent from every edit view; trivial against existing
  `create`.
- **Duplicate block** inside a `blocks` field, plus the common block-editing
  patterns (reorder, collapse all, move between positions).
- Validation error summary at document level, jump-to-field.
- Keep the sticky header work already landed.

**Out of scope:** concurrent-edit conflict resolution. Already specified in
`backlog.md` ("Concurrent-edit conflict-resolution UX") as three separable pieces
with a design; it needs its own spec and is not a 0.1.0 gate.

**Acceptance:** navigating away from a dirty form prompts; duplicating a document
and a block both work and are covered by `viewSuite`.

---

## I — List-view overhaul

The capability already exists server-side and is simply not reached. `find`
supports `withIndex → order → filter → take` applied in that order
(`api/find/server.ts:382-394`), and a `search` query already backs the media
library's debounced input (`MediaLibaryGrid.tsx:50-72`).

### Scope

Each bullet **reintroduces the config A deleted, together with the control that
consumes it** — that pairing is the whole reason A deferred them rather than
half-wiring them.

- **Search** per collection — collection list views have none today; only the media
  grid does. Reuse its debounced pattern.
- **Sort** — re-add `defaultSort` and the per-column `enableSorting` flags, thread
  `order` through the paginated query, and give headers real affordances. `find`
  already applies `order` after index selection (`api/find/server.ts:389-391`), so
  the server half exists.
- **Column visibility** — re-add `defaultColumns` and the per-column
  `enableHiding` flags, with the menu that reads them.
- **Page size** control honoring the `pageSizeOptions` A wired.
- **Saved views** — needs a `vex_`-prefixed preferences table per A's convention.
- Row actions beyond delete; bulk actions beyond delete.

**Acceptance:** a 200-document collection is navigable by search and sort without
Load-More mashing; every `admin.table` key that exists has a consumer, and every
control has a config key; saved views persist per user.

---

## J — Responsive / mobile UI pass — final gate

Runs **last**, on the finished surface.

> **Narrowed by `2026-09-13-discord-post-readiness` Steps 3-4**, which audited every
> admin view plus all of `apps/www` at **375 / 390 / 768 / 1024** before the Discord
> post. J is therefore no longer a first audit of those surfaces — it is a
> *regression* pass over everything C through I changed, plus the widths that spec
> skipped. Read its committed screenshot matrix first; re-auditing what it already
> cleared is waste.

### Method — be realistic about tooling

**Responsively is an Electron desktop app; an agent cannot drive it.** Use it for
your own eyeballing. The agent-drivable path is the `browser` tool driving real
Chromium at fixed viewports, with `tab.ariaSnapshot()` / `tab.screenshot()` per
breakpoint per surface. That produces a reproducible matrix an agent can diff and
fix; Responsively is the human cross-check on top.

### Viewport matrix

| Width | Why | Status |
|---|---|---|
| 375 | iPhone SE / mini — narrowest realistic | covered pre-post; regression only |
| 390 | iPhone 14/15 — most common phone | covered pre-post; regression only |
| 640 | `sm` boundary | **new in J** |
| 768 | `md` boundary — **also `MOBILE_BREAKPOINT` in `use-mobile.ts`**, iPad portrait | covered pre-post; regression only |
| 1024 | `lg` boundary, iPad landscape | covered pre-post; regression only |
| 1280 | `xl` boundary | **new in J** |
| 1536 | `2xl` boundary | **new in J** |

Test *at* and *one pixel either side of* each boundary. The 768 case stays
load-bearing even as a regression check: `useIsMobile` flips the sidebar between an
inline rail and a `Sheet` overlay exactly there (`ui/sidebar.tsx:69, 93-94, 121,
165`), and specs G and H both touch the surfaces either side of that flip.

**Surfaces new to J regardless of width** — nothing pre-post could have covered
them, because they did not exist: the live-preview panel (E), the richtext editor
and toolbar (D), version history and the draft/publish controls (C), every input
reworked by G, the edit-view additions from H, and the list-view search, sort,
column-visibility and saved-views controls from I.

### Surfaces

**Admin (`packages/react`)** — `AdminLayout`, `AdminSidebar`, `AdminTopNav`
breadcrumbs, `DashboardView`, `CollectionListView`, `CollectionEditView`,
`MediaCollectionListView`, `MediaCollectionEditView`, `GlobalsListView`,
`GlobalEditView`, `UnauthorizedView`, `CreateDocumentModal`, `CreateMediaModal`,
`MediaUploadForm` (two-step, accordion), `MediaLibaryGrid`, `DataTable` +
`DataTableBulkActions` (only auditable *after* A unhides it), all 13 field inputs,
and the live-preview panel from E.

**Marketing (`apps/www`)** — every block, and its `create-vexcms/templates/marketing-site` twin.

**Docs (`apps/docs`)**.

### Known suspects, from reading the code

- `DataTable` wraps in `overflow-x-auto` (`DataTable.tsx:225`) — horizontal scroll
  is the fallback, not a mobile design. A wide collection on a 375px screen is the
  worst case in the product.
- Sticky edit-view headers with Save/Cancel — vertical space is scarce at 375.
- `AdminTopNav` breadcrumbs reverse chevron direction from `sidebar.side`
  (`:113-115`) and render a three-level trail; behavior when it overflows is
  untested.
- Two-step media upload accordion with per-file metadata forms.
- The bulk-action floating bar, once it renders.

### Constraint

Tailwind class changes and responsive polish only. **A layout bug requiring a
component rewrite gets filed, not fixed here** — J is a gate, not a redesign, and
must not become the spec that slips the release.

### Acceptance

- Every surface above screenshotted at all 7 widths with no horizontal overflow
  (except the data table's deliberate scroll region), no clipped controls, no
  unreachable primary action, no overlapping text.
- Every primary action reachable at 375px.
- Sidebar transition across 768 verified in both directions.
- Findings needing structural work filed in `backlog.md` with the audit evidence.

---

## Release mechanics — do not forget

1. **Exit prerelease mode.** `.changeset/pre.json` has `mode: "pre"`, `tag: "alpha"`
   and **15 queued changesets**. 0.1.0 needs `changeset pre exit`.
2. **`release.yml` fires on push to `master`** (`.github/workflows/release.yml:4-6`);
   `.changeset/config.json#baseBranch` is `master`. Current working branch is `dev`.
3. **Publishing must go through pnpm** — `npm pack` leaves `"catalog:"` unresolved
   in packed manifests. `scripts/check-packed-manifests.mjs --packed` is the gate.
4. **`scripts/verify-scaffold.mjs`** is the end-to-end proof: pack all 8 packages,
   scaffold both templates from the tarballs, install, typecheck, build, and assert
   the prerender manifest plus `/sitemap.xml` and `/robots.txt`. Green before tag.
5. **Decide `@vexcms/richtext-plate`'s fate before tagging.** If D slips, it must
   not ship at 0.1.0 exporting three orphan types — pull it from the fixed
   changeset group rather than publish an unusable package.

## Explicit 0.1.0 non-goals

State these publicly so nobody asks:

- **`ui` and `tabs` fields.** Cut with reason in the launch-readiness spec (D8):
  both change core invariants rather than adding a leaf. `ui` is non-persisted and
  must be skipped by schema generation, form validation, *and* column generation;
  `tabs` hoists child fields to the parent. Not a polish item. `textarea`, `email`,
  and `json` are leaf fields and are the cheap way to raise the field count.
- Concurrent-edit conflict UX, tag-based cache control, server-side revalidation
  dispatch, decorative-image alt escape hatch, RBAC generic type-narrowing — all
  already assessed in `backlog.md`.
- Form builder, content scheduling, API keys, team management, audit log,
  S3/R2 adapters, TanStack Start adapter, plugin system, multi-component
  workspaces.
