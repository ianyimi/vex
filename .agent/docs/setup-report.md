# Harness Setup Report — vexcms
> Written by `harness init finish` at commit 3a18c95. Review it, then ask the
> agent questions, correct errors, or request changes — everything here is adjustable.

## What was configured

- **Platforms**: omp, claude — bridges generated by `harness sync`
- **Workflow**: high-care, importance high, commit mode `message-only`
- **Standards domains**: frontend, backend, core, testing, tooling, docs
- **Modules on**: specs, tasks, session_log, roadmap, design, decisions, research, env_manifest, naming_conventions
- **Modules off**: (none)
- **Pinned dependencies**: convex, better-auth, @convex-dev/better-auth, convex-helpers, platejs, @tanstack/react-form, @base-ui/react, zod, @tanstack/react-table

## How agents will use this harness

- Every session starts with `harness doctor` (auto via the platform hooks) and `harness state`.
- Workflows are skills in `.agent/skills/` — invocable as slash commands (`/harness-init`,
  `/dev-spec`, `/sync-spec`, `/commit`, `/summary-prompt`, …) or by natural language.
- The harness-keeper advisor is ON (OMP): a cheap watcher that turns your feedback
  into harness updates, logged in `docs/harness-changelog.md` and relayed back to you.
- Before touching code, agents query the code map (`harness context --for "<task>"`) to load
  exactly the standards that govern the files they will change.
- Learnings flow back into the harness (see `docs/harness-guide.md`, "Where knowledge goes"),
  and `harness sync` recompiles the map after standards change.
- `/commit` runs the project's commit checklist
  (`.agent/skills/commit/references/commit-checklist.md`) before anything ships upstream.

## What to review now

- `docs/harness-guide.md` — how this harness is organized
- `docs/standards/` — the practices recorded for this project (discovery findings below)
- `docs/standards/naming-conventions.md` — the naming rules agents will enforce
- `.agent/skills/commit/references/commit-checklist.md` — your pre-push gate

## What discovery found

Seven parallel scouts (one per standards domain + naming) swept the codebase and mined the
old `.pi/` harness. Every mined claim was verified against current code before adoption.

### Standards written (22 files under `docs/standards/`)

- **frontend/** — components-and-shadcn, forms, data-fetching, tables, css-and-tokens
- **backend/** — convex-functions, auth-adapters, file-storage-adapters, schema-generation
- **core/** — adding-a-field-type (verified step-by-step against `fields/upload/`),
  type-generation-strategy, hkt-adapter-architecture, field-config-conventions
- **testing/** — test-structure, field-type-testing, convex-integration-testing
- **tooling/** — workspace-packaging, tsup-and-exports, cli-architecture, release-and-versioning
- **docs/** — jsdoc-conventions, docs-site-conventions
- **naming-conventions.md** — 15 machine-checkable rules (8 added by discovery);
  `harness struct --check` passes clean.

### Drift found while mining `.pi/` (code wins)

- `.pi/agent-docs/standards/backend/effect.md` — **completely stale**: Effect is not used
  anywhere (zero imports, zero deps). Excluded from the new harness.
- Playwright E2E docs in old testing standards — **stale**: root `test:e2e` script exists but
  no Playwright config/specs are present. Recorded as "don't claim E2E coverage".
- tech-stack: TypeScript 5.9 → **6.0** (installed 6.0.3); better-auth pin `>=1.4.9 <1.5.0` →
  catalog `^1.5.0`; UI primitives are Base UI + shadcn CLI, not classic shadcn/ui.
- tsup "global banner" phrasing corrected: the real pattern is an ARRAY of configs, banner
  only on the client config (`packages/next/tsup.config.ts`).
- starlight-typedoc is currently DISABLED in `apps/docs/astro.config.mjs` (types not stable).
- All packages currently ship `dts: false` ("CPU issue") — dev types flow via the `source`
  export condition.
- `scripts/sync-template-versions.mjs` contains stale package names (`@vexcms/admin-next`,
  `@vexcms/ui`, `@vexcms/richtext`) — flagged for refresh.
- Debug hierarchy reordered per developer: tmux pane FIRST; agents never open browsers.

### Old-harness content migration map

| Old location | Disposition |
|---|---|
| `.pi/AGENTS.md` | Superseded by `.agent/AGENTS.md` + bridges; worktree rules + design pointer migrated |
| `.pi/agent-docs/product/{tech-stack,dev-processes,mission,roadmap}.md` | Migrated (corrected) into `docs/product/` |
| `.pi/agent-docs/product/{implementation-plan,v0.1.0-launch-roadmap,maprios-migration-todo}.md` | TRUSTED sources — stay in place, referenced from roadmap |
| `.pi/agent-docs/standards/**` (incl. global/, frontend/, backend/, testing/) | Verified content migrated into `docs/standards/`; effect.md dropped as stale |
| `.pi/agent-docs/standards/{jsdoc-conventions,adding-a-field-type,developer-preferences}.md` | Core content migrated (docs/jsdoc-conventions.md, core/*, tooling/*, frontend/*) |
| `.pi/prompts/` + `.pi/skills/` (dev-spec, sync-spec, commit, document, changeset) | Superseded by `.agent/skills/`; changeset flow folded into commit checklist |
| `.claude/commands/{dev-spec,sync-spec,commit,document,debug,learn,research,review}.md` | Superseded by harness bridges (left untouched by sync, flagged as conflicts) |
| `.claude/commands/add-vexfield.md` | Superseded by `docs/standards/core/adding-a-field-type.md` |
| `.claude/commands/agent-os/`, `memory/`, root `REBUILD-*.md`, `.rebuild/` | Deprecated rebuild-era artifacts — cleanup candidates |
| `.pi/design/**` | Stays — live design source of truth, referenced from frontend/css-and-tokens.md |
| `.pi/agent-docs/specs/**`, `implementation-log/**` | Historical record — stays in place |

### Known noise / deliberate gaps

- `harness doctor` emits ~35 WARNs of the form "tech-stack.md lists X but package.json does
  not declare it" — the check compares against the ROOT package.json, but this is a pnpm
  catalog monorepo; workspace deps live in `pnpm-workspace.yaml` + per-package manifests.
  Non-fatal, safe to ignore.
- `richtext-plate` and `next` packages have no tests — recorded, not fixed.
- Naming inconsistencies flagged (not renamed): `MediaLibaryGrid.tsx` (typo),
  `use-mobile.ts`, `blocks/logo-cloud.ts`.
- Dependency source clones: all 9 registered deps cloned via `harness deps clone`.

### Cleanup performed (developer-approved, 2026-08-08)

Deleted after content migration: `.pi/AGENTS.md`, `.pi/prompts/`, `.pi/skills/`,
`.pi/agent-docs/standards/` (incl. stale effect.md), root `memory/`, and the superseded
`.claude/commands/` files (dev-spec, sync-spec, commit, document, debug, learn, research,
review, add-vexfield, feature-checklist, guide, copy-commit, copy-commit-body, harness-init,
agent-os/). `harness sync` then regenerated managed command bridges; doctor exits 0.

Kept intentionally: `.pi/agent-docs/{product,specs,implementation-log,bug-reports,notes}/`,
`.pi/design/` (live design source of truth), trusted product docs, root REBUILD-* /
`.rebuild/` (project files, not agent docs), and `.claude/skills/excalidraw-diagram/`
(unrelated personal skill — it blocks harness from symlinking `.claude/skills`; relocate it
if you want that symlink, otherwise the WARN is harmless).
