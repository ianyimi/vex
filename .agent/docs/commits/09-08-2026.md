# Commits — 2026-09-08

Two commits today, in order. Each run contributes ONE commit covering the entire working tree
(P-016 — no splitting until the first official release), so each section's `git add -A` is
complete on its own. The 03:31 commit is already in history as `10d3809`; the 21:50 section is
the one to commit now.

## 03:31 — fix(fields): enforce required and fix a11y across every field type

Raw file: `.agent/docs/session-log/2026/09/2026-09-08.commit.md`

```
git add -A && git commit -F .agent/docs/session-log/2026/09/2026-09-08.commit.md
```

fix(fields): enforce required and fix a11y across every field type

Closes out the 2026-09-07-react-test-suite-fixes spec (Steps 1-13), the fix pass for the
353 assertions the 2026-09-04 test-suite spec surfaced and deliberately left red.
packages/react goes from 353 failing to 917/918 (1 intentional skip), apps/test 326→711/711,
apps/www 6→27/27, packages/core stays 935/936 (1 pre-existing unrelated skip). findings.md,
the spec's own defect ledger, is now empty.

**Core schema (CORE-1/2/3).** Every packages/core/src/fields/*/inputSchema.ts composes
min/max onto the required branch instead of reassigning over it, so a required field with
min/max configured keeps every check instead of silently losing the "required" message
(text, number, checkbox, select, date, array, blocks, relationship, upload, url, group,
color). url checks emptiness before URL format so an empty required field reports "This
field is required." instead of "Invalid URL" (CORE-2). date's schema and UI both honor
min/max (CORE-3).

**Missing/dangling labels (LABEL-1).** date, select, checkbox, upload, relationship's
triggers now carry a real id/aria-labelledby the shared test contract's getControl can
resolve. array/group/blocks — which have no single control for a <label for> to point at —
switch to role="group" + aria-labelledby on a plain label span, reproducing FormLabel's
visual output without a dangling label. Dropped aria-required from every role="group"
wrapper (deviates from the spec's literal code sample): axe flags it as not ARIA-allowed on
that role, and required-ness is already conveyed by the group's own visible asterisk.

**Missing aria-required (A11Y-1).** Every field type now forwards required/aria-required to
its control — text, url, color, number, date, select, checkbox, upload, relationship, plus
the container wrappers via their visible asterisk.

**Field-specific fixes.** checkbox's control is findable by its own label and no longer
suppresses the required asterisk (A11Y-2, UI-3). select's remove badges get an accessible
name (A11Y-4). relationship's chip-remove disabled guard was inverted, and its trigger never
wired onBlur at all - both fixed, plus role="combobox" (needed once id={name} was added: a
native <label for> pointing at a <button> was overriding the trigger's own dynamic content
as its accessible name, so "Alpha" was announcing as the static field label instead).
ARCH-1's collection-level relationship-preview branch was permanently dead
(sanitizeConfigForClient strips every function before a client component sees it) - removed
from resolveRelationshipPreview, CollectionConfig/CollectionConfigInput's
admin.components.preview, and the docs. PKG-1: <AppForm> now accepts a form carrying
validators. Thirteen smaller UI-* fixes: number's cleared-value ?? 0 fallback, upload's
missing FormDescription/FormError and aria-required forwarding, array's atMax guard and
dropped stray index prop, group/blocks' AccordionItem now forwards disabled={readOnly}.

**Beyond the 24 catalogued defects**, confirmed with the developer before applying: upload's
single-select fields could still stage multiple files via drag-and-drop (bypasses the
dropzone's own multiple attribute); array's remove-button aria-label was purely positional,
so an array nested inside an array produced two identically-labelled buttons - scoped to
"Remove item N from <field label>"; blocks/Input.tsx and FormBlocks.tsx both rendered
FormError, duplicating every validation message - removed the outer duplicate. Also fixed
two pre-existing console warnings unrelated to the catalogue: checkbox's
uncontrolled-to-controlled Base UI warning and color's PopoverTrigger nativeButton={false}
mismatched against the real <button> in its render prop.

**Harness fixes**, in the shared testing/fieldInputContract.ts and
testing/nestedFieldContainer.ts used by all 12 field types: getControl/getLabelNode no
longer collide on a container's own group label containing a nested sub-field's label as a
substring ("Tags" vs "Tag"); the generic nested-container disabled check now also accepts
aria-disabled="true", matching Base UI's focusable-when-disabled AccordionTrigger pattern;
expectControlValue no longer hands a stringified value to jest-dom's toHaveValue for
type="number" inputs; several tests raced Base UI's CSS-driven Popover/Dialog exit-animation
unmount and now use waitFor; two stale tests that pinned the pre-fix CORE-1/CORE-2 behavior
are updated to assert the now-correct precedence. fieldInputContract.ts's shared renderField,
nestedFieldContainer.ts's renderContainer, and upload's own Input.test.tsx all wired a bare
QueryClient with no queryFn plus a real ConvexReactClient pointed at an unreachable
placeholder URL - every upload/relationship query fired through any of the three (the
harness's "network-via-react-query" representatives) logged "No queryFn was passed" and
updated state outside act() after each test finished. Neither warning ever affected an
assertion (resolved content is checked only through each field's own real convex-test
bridge in its dedicated `extra` block), so all three QueryClients now register a queryFn
that never resolves.

Rebuilt packages/core, react and next before Step 13's apps/test verification - it consumes
the published dist via @vexcms/next/testing's subpath, not live source, and a stale build
produced 303 false failures shaped exactly like ADR-009's already-fixed dual-AppFormContext
defect class.

**SSG/ISR parity for apps/test.** Rewrites apps/test's public routes to match apps/www's
tested pattern from the 2026-09-04-seo-prerendering-and-lifecycle-hooks spec: a shared cached
client (src/lib/vex.ts, createVexServerClient), a publishedSlugs query (convex/pages.ts),
routes.map wired into vex.config.ts, generated sitemap.xml/robots.txt/revalidate route, and a
(site) route group split out of (frontend) so home/[slug] prerender as static/SSG instead of
dynamic. Root-caused via bisection: ThemeStyle.tsx (rendered from every route's shared layout)
read Convex through fetchQuery, which hard-codes cache: "no-store" and forces every route
beneath it dynamic regardless of what the page itself does - switched to the same cached
client every other read already uses. apps/test now builds with the same route table shape as
apps/www (○ static, ● SSG on [slug], ƒ only on /admin, auth, and the revalidate handler).

**Docs: a Next.js sidebar subsection, and two real fixes found while writing it.**
apps/docs/astro.config.mjs gains a manual "Next.js" sidebar group (Quickstart, Caching/SEO)
alongside the existing autogenerated "Guides" - the two pages stay at their existing URLs
(sidebar.hidden: true) rather than moving, since roughly fifteen hardcoded
docs.vexcms.dev/guides/quickstart/ URLs live in apps/www's and the marketing-site template's
seed data and block-config defaults. caching-and-seo.mdx gains a setup checklist and a new
gotcha section documenting the exact ThemeStyle.tsx bug above: a fetchQuery call anywhere in a
shared layout poisons every route beneath it, not just the page calling it. theming.mdx's own
ThemeStyle.tsx example carried the identical anti-pattern (fetchQuery instead of the cached
client) - fixed to match the real implementation. Building the docs surfaced a real type bug
independent of the docs themselves: CreateDocumentModal's CollectionConfig<TSlug> bound TSlug
into the wrong generic position (CollectionConfig's first parameter, not its third) - fixed to
match CollectionListView's established three-parameter convention.

**Deferred, recorded in backlog.md.** CodePane (apps/www) is a deliberately-async Server
Component rendered through PageContent.tsx's "use client" tree - confirmed non-fatal today
(content renders correctly via SSR) but flagged by React as unsupported. The fix requires
pre-highlighting server-side and losing code panes' live-reactivity; developer chose to defer
rather than take that tradeoff inside this commit.

Spec: .agent/docs/specs/2026-09-07-react-test-suite-fixes/spec.md (fields/a11y);
.agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec.md (SSG pattern
followed for apps/test)
Log: .agent/docs/session-log/2026/09/2026-09-08.log.md

## 21:50 — feat(react)!: ship the exported test kit and fix every defect it found

Raw file: `.agent/docs/session-log/2026/09/2026-09-08.commit.2.md`

```
git add -A && git commit -F .agent/docs/session-log/2026/09/2026-09-08.commit.2.md
```

feat(react)!: ship the exported test kit and fix every defect it found

Two specs land together: `2026-09-08-react-coverage-expansion` built the suite and recorded
what it found without fixing anything, then `2026-09-08-react-bug-fixes` cleared all 18
recorded defects. Splitting them would leave a commit whose own tests are red on purpose.

**Exported test kit.** `@vexcms/react/testing` — re-exported wholesale as the new
`@vexcms/next/testing` subpath — now ships `runVexReactSuite`, one call that runs the entire
admin contract against a consumer's own build, plus the factories behind it:
`runFieldCellContractSuite`, `runColumnDefSuite`, `runShellSuite`, `runViewSuite`,
`runDataTableSuite`, `runModalSuite`, `runMediaSuite` and `runHooksSuite`. A `sections` option
selects categories, `custom` drives project-authored field fixtures through the same contract,
and `access` threads a real `VexAccessConfig` so RBAC-gated paths render under the consumer's
own matrix. `apps/test` deliberately omits `sections` so it runs everything and picks up future
sections automatically; `apps/www` keeps a fast `["shell"]` subset. Statement coverage on
first-party `packages/react` code went from 58.2% to 91.62%, gated in `vitest.config.ts` at a
fixed 80% for statements plus measured floors for the rest.

**List-view cells.** Every type renders the em-dash placeholder for a `null`/`undefined` value
instead of crashing — `text` and `array` threw a `TypeError` on `.length`, and a throw in a cell
renderer takes down the whole table render. A `date` cell holding epoch `0` now renders
1970-01-01 rather than treating a real timestamp as an absent value. All 12 types honor
`isTitleField` by wrapping their content in an edit link, so a `date` or `select` title column is
finally clickable; `relationship` and `upload` wrap every return path, loading states included.
Values longer than 77 characters truncate with the full text on `title`, which `relationship`,
`select`, `array`, `blocks`, `upload` and `group` previously left unreachable by tooltip.

**Selection and pagination hooks.** `useTableSelection`'s `"all"` mode reads `selectedIds` as an
exclusion set, so "select all, then untick one" genuinely deselects that row instead of
reporting it as selected — which would have deleted an excluded row once bulk-delete returns.
`toggleRow`'s `onSelectionChange` reports the post-change `mode` rather than a stale closure
value. `usePaginatedQuery.loadMore()` reveals one page per call instead of fetching page N and
exposing it only on the next call, and `isDone` derives from the query's own
`isLoading`/`isError`, so a pending or rejected first page is no longer indistinguishable from a
complete empty collection.

**Modals.** `Modal` gains a `dismissible` prop that cancels Base UI's own close handling, gating
Escape, backdrop clicks and every `DialogClose` centrally instead of per trigger.
`CreateDocumentModal` pairs it with a synchronous in-flight flag set before TanStack Form's async
validation resolves: a rapid double-click creates one document instead of two, and Escape
mid-submit no longer closes the dialog while the write still lands.

**Views and media.** `MediaCollectionEditView`'s Save/Cancel now read `!canEdit ||
isDefaultValue`, and `MediaCollectionListView` resolves a create-action permission for its Upload
button — both matching their non-media counterparts. `MediaUploadDropzone` filters dropped files
against a safe-media MIME allowlist (it previously accepted executables) and keeps the first file
of a multi-file drop instead of rejecting the batch. `FilePreview` falls back to the filename
when `alt` is empty; the old `??` never fired, since `alt` is a required `string` that is `""`
when unset, so every image without alt text rendered `alt=""`.

**Rules of Hooks and storage robustness**, found while hardening the suite rather than by a
finding. `ThemeProvider` reads and writes `localStorage` through guarded helpers — the unguarded
access threw inside the mount effect and unmounted the whole admin shell wherever Web Storage is
unavailable, including Safari private mode and "block all cookies". `upload`'s cell calls
`useQuery` unconditionally with `get`'s own `skip` sentinel instead of after an early return, and
`MediaUploadForm` owns its accordion state in a real `StagedFilesEditor` component instead of
inside a render prop; both made React log "Rendered fewer hooks than expected" and abandon
concurrent rendering. `FormArray`/`FormBlocks` pass `isDragDisabled` when read-only, so a
read-only list stops registering drag handles it never renders.

**Dead code removed.** `usePagination` and `DataTablePagination` had zero production consumers —
both list views paginate with `usePaginatedQuery` + `onLoadMore`, and no view ever rendered
page-number pagination. Deleted with their tests rather than fixing defects in unreachable code.

**Test output is silent again.** `apps/test` and `apps/www` could not run at all: their
`coverage.reporter` listed `"default"`, a *test* reporter, so istanbul threw
`Cannot find module 'default'` before a single test executed. Beyond that, `expectNoA11yViolations`
runs axe inside `act`, the dropzone's negative paths wrap their settle window, and the
double-submit test awaits the dialog's close — a real assertion, since closing is the last thing
the create's success path does. 13 stale `// FAILS:` comments became regression-guard notes now
that those assertions pass. `scripts/vercel-build-check.mjs` skips tracked files deleted in the
working tree instead of crashing on them, so the pristine-clone check runs on a tree with
deletions.

**`@vexcms/core`'s own diagnostics no longer leak to stderr.** Its suite printed eight
`[vexcms]` dev warnings per run. Two of the three causes were tests that deliberately trigger
a diagnostic — the access-index collision in `find/server.test.ts` and the two "dev warning
only" degenerate configs in `access/config.test.ts` — but let it print instead of following
the `vi.spyOn(console, "warn")` convention the same file's own "dev-mode warnings" block
already uses; they now spy and assert the message, so the diagnostic is a tested contract
rather than output. The third was a real test smell: `publishedSlugs/server.test.ts` passed
`access: { bypass: true }` on four calls that thread no config, and `resolveAccessCall` was
correctly reporting that the flag changed nothing. Dropped, with a note on why the sitemap
JSDoc's advice does not apply to a config-less call. Every other package — better-auth,
file-storage-convex, cli, create-vexcms, next, and both apps — was already silent.

**Released as `0.1.0-alpha.14`.** One consolidated changeset covered this landing plus the two
commits that shipped without one — `fix(fields)` (10d3809) and `test(react)` (60646d3) — and
`pnpm version:packages` has consumed it: `@vexcms/core`, `@vexcms/react` and `@vexcms/next`
carry the minor bump, every package's CHANGELOG gained the entry, and `create-vexcms`'s
template pins were synced to the new range.

BREAKING CHANGE: `defineCollection` now singularizes a collection's `slug` before title-casing
it for the default `labels.singular`, and derives `labels.plural` as the title-cased slug
itself instead of running it through `plural()` again. For slug `posts`, `singular` was `Posts`
and is now `Post`; `plural` was `Postses` and is now `Posts`. Irregular-suffix slugs were hit
the same way — `categories` was `Categorieses`, `media` was `Medias`. Set `labels` explicitly to
pin the old values. Separately, `usePagination`, `UsePaginationProps`, `UsePaginationReturn`,
`DataTablePagination` and `DataTablePaginationProps` are no longer exported from
`@vexcms/react`; use `usePaginatedQuery` for cursor pagination.

Spec: .agent/docs/specs/2026-09-08-react-coverage-expansion/spec.md, .agent/docs/specs/2026-09-08-react-bug-fixes/spec.md
Log: .agent/docs/session-log/2026/09/2026-09-08.log.md
