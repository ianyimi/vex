# Commits — 2026-09-02

> One commit this run (project overlay P-016: one commit per run covering the
> entire working tree). The file list below covers every path in `git status`.

## 00:52 — feat(www): build the real marketing site, generate nested select unions

**Committed:** `84f09e4`

Raw message: `.agent/docs/session-log/2026/09/2026-09-02.commit.md`
Files: 3

```sh
git add \
  '.agent/docs/session-log/2026/09/2026-09-02.log.md' \
  '.github/workflows/ci.yml' \
  '.github/workflows/release.yml' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-02.commit.md
```

---

### Full message

feat(www): build the real marketing site, generate nested select unions

**The site.** `apps/www` is a new workspace member scaffolded from the published
marketing template, and it is the first thing in this repo built from a written
design rather than improvised: a sitemap and per-page content spec
(`2026-09-01-www-content-spec`) drove a design brief, and the returned design in
`.agent/docs/design/www/` is now normative for both the app and the template it
ships as. The IA narrows to three pages — `/`, `/features`, `/roadmap`. Three
blocks are new (`Stats`, `CodeShowcase`, `Split`), all eight existing renderers
were rebuilt against the design, and `Hero` grows a `variant` so the interior
pages reuse it compactly instead of forking a second hero. Seven shared
components carry the repeated shapes (`Container`, `SectionHeader`, `BrandMark`,
`CopyButton`, `InstallCommand`, `CodePane`, `MediaImage`). `CodeShowcase`
highlights with shiki in a server component, so no highlighter reaches the
client. Everything is field data on seedable blocks, never bespoke per-page JSX,
which is what lets one `pnpm seed` stand the whole site up.

**Nested `select` fields now generate literal unions.** `childInterfaceType()`
resolves the TypeScript type for a field nested inside another field, and the
three builders that needed it — blocks, groups, arrays — each duplicated the
logic and all three fell through to a generic `string[]`. Collections had been
emitting proper unions all along. A `select` inside a block now types as
`("full" | "compact")[]`, so renderers derive from generated types instead of
re-declaring hand-written unions beside them; `RoadmapItemStatus` was the last
such duplicate and is gone. `Roadmap.status` also gains a fifth value, `future`,
separating committed-but-unscheduled work from open research.

**`select` fields were unusable below the fold, and the cause was none of the
obvious candidates.** Opening one on a scrolled admin page threw the viewport
1341px and left the trigger off-screen. Scroll locks, Base UI's
`alignItemWithTrigger` page-scrolling, and a global `scroll-behavior: smooth`
were all real members of the defect family but none was the writer; that guess
cost three rounds and is now recorded as an anti-pattern. A floating popup is
positioned a frame or two *after* it mounts, and until then sits at the end of
`<body>`, thousands of pixels from its trigger. cmdk `scrollIntoView`s its
highlighted item in a mount-time layout effect inside that window, walking every
scrollable ancestor and dragging the page to the popup's pre-position location.
`scrollIntoView` takes no `preventScroll`, so it cannot be made safe — only a
no-op: `MultiSelectContent` holds content at `display: none` until positioned,
then reveals, focuses with `preventScroll`, and performs the item scroll itself.
Verified against the running admin edit form at three viewport positions through
full open/pick/close cycles: zero movement. Two regression tests pin the reveal
and focus behaviour.

**The admin gate no longer fails open.** `useCanAccessAdminPanel()` evaluates the
same `canAccessAdminPanel` predicate the server route uses, so the header stops
guessing at role names. It deliberately does *not* defer to `hasPermission`,
which returns `true` when handed no access config — correct for a server guard,
wrong on a public route where the absence only means no provider is mounted. The
hook fails closed on missing context or user.

**Admin readability.** Every collection and global declares an `admin.icon` and
every field an explicit `label`. The worst offender was repeatable rows:
`FormLabel` falls back to a field's path when it has no label, and `group()`
defaults `label` to `""` (only `defineCollection` infers one), so array items
rendered as `[10] - blocks[3].items[9]`. The `group()` passed as each array's
`items` is now labelled, and rows read `[10] - Roadmap Item`.

**Five template defects that only a real scaffold could surface.** Typecheck and
build passed throughout while a scaffolded project was broken: `turbopack.root`
pinned to the app directory broke every `--monorepo` build; base's
`(frontend)/page.tsx` collided with the marketing home on `/` and won; base's
fail-closed proxy matcher sent `/features` and `/roadmap` to sign-in; `anyApi`
in user-project code discarded the types the generated `api` already carries;
and `<img src={uploadField}>` set `src` to a Convex document id, so no uploaded
logo ever rendered. The overlay installer can only add and overwrite — never
delete, never edit `package.json` — so the route removal and the marketing-only
`shiki` dependency move into a new `finalizeMarketingOverlay()`.
`scripts/template-diff.mjs` now reports app↔template drift against the effective
template file, and the `template-sync` skill runs it instead of eyeballing.

**Verified** under forced, uncached runs: typecheck 15/15, tests 12/12, build
11/11, lint 0 errors, no naming violations, plus fresh scaffolds in both `--yes`
and `--bare` modes. The sticky `GlobalEditView` header rides along from an
earlier session, as this project commits the working tree in one commit per run.

Spec: .agent/docs/specs/2026-09-01-www-content-spec/spec.md
Log: .agent/docs/session-log/2026/09/2026-09-02.log.md

---

## 13:​05 — ci: give apps/test build the env it needs so the gate actually runs

**Committed:** `e350af6`

Raw message: `.agent/docs/session-log/2026/09/2026-09-02.commit.2.md`
Files: 5

```sh
git add \
  '.agent/docs/commits/09-02-2026.md' \
  '.agent/docs/session-log/2026/09/2026-09-02.log.md' \
  '.github/workflows/ci.yml' \
  '.github/workflows/release.yml' \
  '.agent/docs/session-log/2026/09/2026-09-02.commit.2.md' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-02.commit.2.md
```

---

## 14:20 — fix(ci): quote the lint glob so CI and local check the same files

**Committed:** `494ce1b`

Raw message: `.agent/docs/session-log/2026/09/2026-09-02.commit.3.md`
Files: 45

```sh
git add \
  '.agent/.sync-manifest.json' \
  '.agent/docs/standards/anti-patterns.md' \
  '.agent/docs/standards/preferences.md' \
  '.agent/docs/state.md' \
  'package.json' \
  'packages/better-auth/src/index.ts' \
  'packages/core/src/collections/internal/index.ts' \
  'packages/create-vexcms/src/index.ts' \
  'packages/next/src/NextAdminLayout.tsx' \
  'packages/next/src/NextAdminLayoutClient.tsx' \
  'packages/react/src/components/fields/blocks/Cell.tsx' \
  'packages/react/src/components/fields/blocks/columnDef.tsx' \
  'packages/react/src/components/fields/checkbox/columnDef.tsx' \
  'packages/react/src/components/fields/group/Cell.tsx' \
  'packages/react/src/components/fields/relationship/Cell.tsx' \
  'packages/react/src/components/fields/upload/Cell.tsx' \
  'packages/react/src/components/fields/upload/EmptyInput.tsx' \
  'packages/react/src/components/fields/upload/FilledInput.tsx' \
  'packages/react/src/components/form/FormArray.tsx' \
  'packages/react/src/components/form/FormBlocks.tsx' \
  'packages/react/src/components/form/FormDescription.tsx' \
  'packages/react/src/components/form/FormGroup.tsx' \
  'packages/react/src/components/form/InputTag.tsx' \
  'packages/react/src/components/media/FilePreview.tsx' \
  'packages/react/src/components/media/MediaLibaryGrid.tsx' \
  'packages/react/src/components/media/MediaPicker.tsx' \
  'packages/react/src/components/media/MediaUploadDropzone.tsx' \
  'packages/react/src/components/ui/data-table/DataTable.tsx' \
  'packages/react/src/components/ui/datetime/date-picker.tsx' \
  'packages/react/src/components/ui/dnd/Droppable.tsx' \
  'packages/react/src/components/views/GlobalEditView.tsx' \
  'packages/react/src/components/views/GlobalsListView.tsx' \
  'packages/react/src/components/views/MediaCollectionListView.tsx' \
  'packages/react/src/index.ts' \
  'packages/richtext-plate/src/editor/components/EditorContainer.tsx' \
  'packages/richtext-plate/src/editor/components/TableToolbar.tsx' \
  'packages/richtext-plate/src/editor/components/Toolbar.tsx' \
  'packages/richtext-plate/src/editor/components/editorElements.tsx' \
  'packages/richtext-plate/src/editor/components/image/ImagePlaceholder.tsx' \
  'packages/richtext-plate/src/editor/components/image/ImageUpload.tsx' \
  'packages/richtext-plate/src/editor/components/image/useImageUpload.ts' \
  'packages/richtext-plate/src/editor/plugins/createPlugins.ts' \
  'packages/richtext-plate/src/render/components/elements.tsx' \
  'packages/richtext-plate/src/render/components/leaves.tsx' \
  '.agent/docs/session-log/2026/09/2026-09-02.commit.3.md' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-02.commit.3.md
```

---

## 22:40 — fix(core): deny constrained rules when the caller has no user

**Status:** pending — not yet committed.

Raw message: `.agent/docs/session-log/2026/09/2026-09-02.commit.4.md`
Files: 5

```sh
git add \
  '.github/workflows/release.yml' \
  'packages/core/src/access/hasPermission.test.ts' \
  'packages/core/src/access/hasPermission.ts' \
  '.agent/docs/session-log/2026/09/2026-09-02.commit.4.md' \
  '.changeset/anon-constraint-fail-closed.md' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-02.commit.4.md
```

---

### Full message

fix(core): deny constrained rules when the caller has no user

**The crash.** `vexcms.dev`'s admin panel returned a 500 with
`TypeError: Cannot read properties of undefined (reading 'email')` thrown from
inside a `constraints` callback. `anonRole` resolves a sessionless caller to a
real role, so an unauthenticated request reaches that role's rules — and a rule
scoping rows to the caller (`fq.eq("email", user.email)`) dereferenced a user
that was not there. `hasPermission` passed `props.user` through untouched, while
the sibling call site in `resolveAccessRule` already defaulted it. The
`base-nextjs` template ships exactly this shape, so every scaffolded project
carries the same fault, not just this site.

**Why the obvious fix was rejected.** Copying `?? {}` from the sibling stops the
throw and replaces it with something quieter and worse: `convexValuesEqual`
treats `undefined === undefined`, so `eq("email", undefined)` compiles to an
index range matching every row whose field is absent. That trades a loud failure
for a silent widening of access. A regression test pins it — with the naive
fallback the check returns `true` for a row with no email.

**What it does now.** The rule's own behavior decides. When there is no user, one
is substituted that records whether the callback reads from it. A rule that asked
who the caller is gets denied, because there is no answer to give. A rule that
never asked — the read-only `anonRole` case, `fq.eq("status", "published")` —
compiles and evaluates exactly as before. Three tests cover the throw, the
widening, and the user-independent rule that must keep working; the guard was
disabled once to confirm they fail without it.

**Release diagnostics.** The alpha.7 publish failed with
`404 Not Found - PUT https://registry.npmjs.org/create-vexcms` on a package the
account owns, with every package still at alpha.6. `.npmrc` reads
`_authToken=${NPM_TOKEN}`; an unset or empty secret is substituted silently, the
request goes out unauthenticated, and npm answers 404 rather than 401 because it
will not confirm a package exists to a caller it cannot authorise. A preflight
step now fails the workflow before the build with the real reason, prints
`npm whoami`, and warns when the token's package permissions do not cover both
the `@vexcms` scope and the unscoped `create-vexcms` — the granular-token case
that would 404 on that package alone.

Spec: .agent/docs/specs/2026-08-30-launch-readiness/spec.md (WP-5, WP-6)
Log: .agent/docs/session-log/2026/09/2026-09-02.log.md
