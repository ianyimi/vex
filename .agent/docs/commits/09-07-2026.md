# Commits — 09-07-2026

> One commit this run, covering the entire working tree (project override P-016:
> no splitting by concern before the first official release). The `git add` list
> below is exhaustive — it names every path in `git status`, including the harness
> bookkeeping (session log, this ledger, `state.md`, `tasks.md`,
> `directory-structure.md`) which spans the whole run.

## 00:24 — feat(next)!: prerender and cache public pages, purge on admin writes

**Raw message (for `git commit -F`):** `.agent/docs/session-log/2026/09/2026-09-07.commit.md`

**Files:** 133 (80 modified · 53 new)

```sh
git add .agent/AGENTS.md \
  .agent/docs/commits/09-07-2026.md \
  .agent/docs/decisions/ADR-009.md \
  .agent/docs/product/backlog.md \
  .agent/docs/research/nextjs-cache-components-and-tags.md \
  .agent/docs/session-log/2026/09/2026-09-05.log.md \
  .agent/docs/session-log/2026/09/2026-09-06.log.md \
  .agent/docs/session-log/2026/09/2026-09-07.commit.md \
  .agent/docs/session-log/2026/09/2026-09-07.log.md \
  .agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/.implement-state.json \
  .agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec-tasks.md \
  .agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec.md \
  .agent/docs/standards/directory-structure.md \
  .agent/docs/state.md \
  .agent/docs/tasks.md \
  .changeset/seo-prerendering-and-revalidation.md \
  apps/docs/src/content/docs/guides/caching-and-seo.mdx \
  apps/test/convex/vex.schema.ts \
  'apps/test/src/app/(frontend)/PageContent.tsx' \
  'apps/test/src/app/(frontend)/[slug]/page.tsx' \
  'apps/test/src/app/(frontend)/page.tsx' \
  'apps/test/src/app/(vexcms)/admin/layout.tsx' \
  apps/test/src/components/providers/server.tsx \
  apps/test/src/vex.types.ts \
  apps/www/convex/pages.ts \
  'apps/www/src/app/(frontend)/(site)/PageContent.tsx' \
  'apps/www/src/app/(frontend)/(site)/[slug]/page.tsx' \
  'apps/www/src/app/(frontend)/(site)/layout.tsx' \
  'apps/www/src/app/(frontend)/(site)/page.tsx' \
  'apps/www/src/app/(vexcms)/admin/layout.tsx' \
  apps/www/src/app/api/vex/revalidate/route.ts \
  apps/www/src/app/layout.tsx \
  apps/www/src/app/robots.ts \
  apps/www/src/app/sitemap.ts \
  apps/www/src/components/ThemeStyle.tsx \
  apps/www/src/components/providers/server.tsx \
  apps/www/src/lib/metadata.ts \
  apps/www/src/lib/vex.ts \
  apps/www/src/vex.config.ts \
  apps/www/src/vexcms/api.ts \
  packages/core/src/access/constants.ts \
  packages/core/src/api/create/server.test.ts \
  packages/core/src/api/create/server.ts \
  packages/core/src/api/globals/upsert.server.test.ts \
  packages/core/src/api/globals/upsert.server.ts \
  packages/core/src/api/publishedSlugs/server.test.ts \
  packages/core/src/api/publishedSlugs/server.ts \
  packages/core/src/api/server.ts \
  packages/core/src/api/test/convex/schema.ts \
  packages/core/src/api/update/server.test.ts \
  packages/core/src/api/update/server.ts \
  packages/core/src/api/utils.ts \
  packages/core/src/collections/config.test.ts \
  packages/core/src/collections/config.ts \
  packages/core/src/collections/constants.ts \
  packages/core/src/collections/types.ts \
  packages/core/src/collections/utils.test.ts \
  packages/core/src/collections/validator.test.ts \
  packages/core/src/config/config.test.ts \
  packages/core/src/config/config.ts \
  packages/core/src/config/types.ts \
  packages/core/src/index.ts \
  packages/core/src/revalidate/constants.ts \
  packages/core/src/revalidate/index.ts \
  packages/core/src/revalidate/resolveTargets.test.ts \
  packages/core/src/revalidate/resolveTargets.ts \
  packages/core/src/revalidate/types.ts \
  packages/core/src/routes/index.ts \
  packages/core/src/routes/types.ts \
  packages/core/src/types/generateVexTypes.test.ts \
  packages/create-vexcms/templates/base-nextjs/convex/vex.schema.ts \
  packages/create-vexcms/templates/base-nextjs/package.json \
  'packages/create-vexcms/templates/base-nextjs/src/app/(vexcms)/admin/layout.tsx' \
  packages/create-vexcms/templates/base-nextjs/src/app/api/vex/revalidate/route.ts \
  packages/create-vexcms/templates/base-nextjs/src/components/providers/server.tsx \
  packages/create-vexcms/templates/base-nextjs/src/vex.types.ts \
  packages/create-vexcms/templates/marketing-site/convex/pages.ts \
  packages/create-vexcms/templates/marketing-site/convex/vex.schema.ts \
  'packages/create-vexcms/templates/marketing-site/src/app/(frontend)/(site)/[slug]/page.tsx' \
  'packages/create-vexcms/templates/marketing-site/src/app/(frontend)/(site)/layout.tsx' \
  'packages/create-vexcms/templates/marketing-site/src/app/(frontend)/(site)/page.tsx' \
  'packages/create-vexcms/templates/marketing-site/src/app/(vexcms)/admin/layout.tsx' \
  packages/create-vexcms/templates/marketing-site/src/app/layout.tsx \
  packages/create-vexcms/templates/marketing-site/src/app/robots.ts \
  packages/create-vexcms/templates/marketing-site/src/app/sitemap.ts \
  packages/create-vexcms/templates/marketing-site/src/components/ThemeStyle.tsx \
  packages/create-vexcms/templates/marketing-site/src/lib/metadata.ts \
  packages/create-vexcms/templates/marketing-site/src/lib/vex.ts \
  packages/create-vexcms/templates/marketing-site/src/vex.config.ts \
  packages/create-vexcms/templates/marketing-site/src/vex.types.ts \
  packages/create-vexcms/templates/marketing-site/src/vexcms/api.ts \
  packages/file-storage-convex/src/config.ts \
  packages/next/package.json \
  packages/next/src/cache/createVexRevalidateRoute.test.ts \
  packages/next/src/cache/createVexRevalidateRoute.ts \
  packages/next/src/cache/createVexServerClient.test.ts \
  packages/next/src/cache/createVexServerClient.ts \
  packages/next/src/cache/index.ts \
  packages/next/src/cache/types.ts \
  packages/next/src/index.ts \
  packages/next/src/seo/createVexRobots.ts \
  packages/next/src/seo/createVexSitemap.test.ts \
  packages/next/src/seo/createVexSitemap.ts \
  packages/next/src/seo/index.ts \
  packages/next/src/seo/vexMetadata.test.ts \
  packages/next/src/seo/vexMetadata.ts \
  packages/next/src/seo/vexStaticParams.test.ts \
  packages/next/src/seo/vexStaticParams.ts \
  packages/next/tsup.config.ts \
  packages/next/vitest.config.ts \
  packages/react/src/components/RevalidateButton.test.tsx \
  packages/react/src/components/RevalidateButton.tsx \
  packages/react/src/components/index.ts \
  packages/react/src/components/media/MediaUploadDropzone.tsx \
  packages/react/src/components/media/MediaUploadForm.tsx \
  packages/react/src/components/modals/CreateDocumentModal.tsx \
  packages/react/src/components/ui/sidebar.tsx \
  packages/react/src/components/views/CollectionEditView.tsx \
  packages/react/src/components/views/CollectionListView.tsx \
  packages/react/src/components/views/GlobalEditView.tsx \
  packages/react/src/components/views/MediaCollectionEditView.tsx \
  packages/react/src/components/views/MediaCollectionListView.tsx \
  packages/react/src/context/VexRevalidateContext.tsx \
  packages/react/src/context/index.ts \
  packages/react/src/hooks/index.ts \
  packages/react/src/hooks/useVexMutation.test.tsx \
  packages/react/src/hooks/useVexMutation.ts \
  packages/react/src/hooks/useVexRevalidate.test.tsx \
  packages/react/src/hooks/useVexRevalidate.ts \
  packages/react/src/index.ts \
  scripts/regen-template-schema.mjs \
  scripts/verify-scaffold.mjs \
  scripts/verify-seo-routes.mjs

git commit -F .agent/docs/session-log/2026/09/2026-09-07.commit.md
```

---

feat(next)!: prerender and cache public pages, purge on admin writes

Every public route in `apps/www` was server-rendered on demand, serving
`Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate` with a
Convex round trip per request, and `.next/prerender-manifest.json` listed only
`/_global-error`. Two independent causes: the root layout's `AuthServerProvider`
put a `getToken()` cookie read on every route's render path — proven with a probe
page containing no data fetching that still built as dynamic — and `fetchQuery`
(`convex/nextjs`) hard-codes `cache: "no-store"`. Either alone forces a route
dynamic. `/` now prerenders with `s-maxage=3600` and serves `x-nextjs-cache: HIT`,
`/[slug]` prerenders per published slug, and a save in the admin panel purges the
paths that document renders on.

**Cache and SEO surface (`@vexcms/next`).** Two new entry points.
`@vexcms/next/cache` exports `createVexServerClient`, a server-only Convex read
client that never sets `no-store` and dedupes through `React.cache`, so a page and
its `generateMetadata` share one round trip instead of two — which is why each app
exports a single shared instance from `src/lib/vex.ts` rather than constructing one
per file. It also exports `createVexRevalidateRoute`. `@vexcms/next/seo` exports
`vexStaticParams`, `createVexSitemap`, `createVexRobots`, and `vexMetadata`; each
degrades on an unreachable deployment instead of throwing, so a CI build with
placeholder env still produces a valid route table.

**Route mapping (`@vexcms/core`).** `vex.config.ts` takes `routes: { map }` —
document in, public paths out. `resolveTargets` invokes it once per document
*state*, not once per write, so renaming a slug purges both the old and the new
path without the map knowing a rename happened. Named for what it describes rather
than for its first consumer: an admin "View page" link and sitemap URL generation
want the same answer, and neither is a cache concern. The ISR window is
deliberately absent from config — Next reads `export const revalidate` by static
analysis before any module executes, so it accepts only an inline literal in the
route file. See ADR-009.

**Revalidation (`@vexcms/core`, `@vexcms/next`, `@vexcms/react`).**
`POST /api/vex/revalidate` is session-authorized and re-checks `hasPermission`
server-side; it holds no secret and adds no credential the signed-in caller did not
already present. The wire contract (`VexRevalidateChange`,
`VexRevalidateRequest`/`Response`, `VEX_REVALIDATE_BATCH_SIZE`) lives in core
because `@vexcms/react` builds it and `@vexcms/next` parses it, and neither depends
on the other. `useVexMutation` fires the purge after a successful write,
fire-and-forget — a failed purge is a stale page, a failed save is lost work, so it
never delays or fails the mutation. `RevalidateButton` adds a user-initiated purge
whose failures surface, covering what no client-driven purge can reach: Convex
dashboard edits, `npx convex import`, or a tab closed mid-request.

**Automatic `updatedAt` (`@vexcms/core`).** `defineCollection` injects an optional
`updatedAt` field and `create`/`update` stamp it through one shared helper, so the
two write paths cannot drift. Three ownership-based exclusions: `{ timestamps:
false }` opts out, `meta.protected` skips auth-adapter-owned collections entirely,
and a `meta.locked` field the auth adapter declared itself is never overwritten.
The `meta.protected` gate is load-bearing — `jwks` declares no `updatedAt` of its
own, so a per-field check alone added a column to an auth table nothing would ever
populate. Globals carry none: `vex_globals` is one shared `{ slug, data }` table
with no per-global `defineTable`, so there is no column to stamp.

**Templates and gates.** Both `create-vexcms` templates carry the full change and
are re-verified by real scaffolding. `scripts/verify-scaffold.mjs` gained
route-table and SEO assertions read from build manifests plus a `--negative-routes`
self-test that forces a route dynamic and fails if the assertion still passes; its
exit code discriminates, because returning non-zero on both branches would have let
a broken assertion pass CI forever. `base-nextjs/package.json` gained the
`pnpm.overrides` and `peerDependencyRules` block the monorepo already used — a
fresh scaffold's `pnpm install` was already failing with
`ERR_PNPM_PEER_DEP_ISSUES` once `@better-auth/passkey` published a minor whose peer
range excluded the pinned `better-auth`.

**Fixes found while verifying.** `createVexSitemap` resolved `getUpdatedAt` per
call rather than per item, so a row never written through the API produced
`new Date(undefined)` and an unparseable `<lastmod>`. `SidebarMenuSkeleton` derived
its width from `Math.random()` inside a `useState` initializer, which runs during
render on both server and client — a live hydration-mismatch hazard, now a hash of
`React.useId()` that keeps the intended per-row variety.

BREAKING CHANGE: `defineCollection` injects an `updatedAt` field into every content
collection, so `convex/vex.schema.ts` and `src/vex.types.ts` must be regenerated
and the schema pushed — run `vex dev` (or `vex dev --once`) before the next write,
or Convex rejects the stamped column. A collection declaring its own field named
`updatedAt` is now a compile-time and runtime error: rename it, or opt the
collection out with `{ timestamps: false }`.

Spec: .agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec.md
Log: .agent/docs/session-log/2026/09/2026-09-07.log.md

---

## 01:05 — fix(next): commit the lockfile entry for the new vitest devDependencies

> Follow-up to the commit above, which is already in as `bb9acae`. That commit
> shipped `packages/next/package.json`'s new devDependencies without the
> matching `pnpm-lock.yaml` entry, so the PR's Vercel build failed at install.
>
> Only this section is uncommitted — commit it alone. Its list is NOT disjoint
> from the section above: `packages/next/package.json`, `state.md`, the session
> log, and this ledger were all in `bb9acae` and are modified again here. That
> is expected for a follow-up, not a clustering error.

**Raw message (for `git commit -F`):** `.agent/docs/session-log/2026/09/2026-09-07.commit.2.md`

**Files:** 7

```sh
git add .agent/docs/commits/09-07-2026.md \
  .agent/docs/session-log/2026/09/2026-09-07.commit.2.md \
  .agent/docs/session-log/2026/09/2026-09-07.log.md \
  .agent/docs/state.md \
  .agent/skills/commit/references/commit-checklist.md \
  packages/next/package.json \
  pnpm-lock.yaml

git commit -F .agent/docs/session-log/2026/09/2026-09-07.commit.2.md
```

---

fix(next): commit the lockfile entry for the new vitest devDependencies

`packages/next/package.json` gained `vitest` and `@vitest/coverage-v8` when the
package got its first test suite, but no install followed, so `pnpm-lock.yaml`
never recorded them. Vercel installs with `--frozen-lockfile` and refused the
tree: `ERR_PNPM_OUTDATED_LOCKFILE`, `2 dependencies were added: vitest@catalog:,
@vitest/coverage-v8@catalog:`.

The drift was invisible locally because `.npmrc` sets `shamefully-hoist=true`.
`vitest` resolved from the hoisted root, so `@vexcms/next`'s tests ran and
typecheck, test, build, and lint all passed against a lockfile that could not
install from scratch. Regenerated with `pnpm install --lockfile-only` — six
added lines, confined to `packages/next` — and verified by installing a pristine
copy of tracked plus untracked-but-not-ignored files, with no `node_modules`,
under `--frozen-lockfile`. Also sorts that file's `devDependencies`
alphabetically, matching every sibling package.

**Close the gap that let it through.** Nothing in the commit checklist ran a
frozen-lockfile install, and `scripts/vercel-build-check.mjs` — written for
exactly this class of fault, after two deploys failed on faults invisible in a
working copy — was never a checklist item despite its own docstring saying to
run it before committing. Both are now "Must pass" entries, the first scoped to
"whenever any `package.json` changed", the second to "when `apps/www` or any
package it imports changed", with a note that a pristine tree has no
`.next/cache` so `next/font` needs network to fetch Geist and an offline failure
there is environmental rather than a code fault.

Spec: .agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec.md
Log: .agent/docs/session-log/2026/09/2026-09-07.log.md

