# Commits — 2026-09-04

> Four commits today, one per run (project overlay P-016: one commit per run
> covering the entire working tree). The first three fix the deploy the
> previous one shipped; all three have landed as `7200c67`, `b1b4340`, and
> `19603f6`. Only the run below is outstanding.

## 12:02 — fix(www): deploy convex on build, add seed reinit and logout control

**Committed:** `7200c67`

Raw message: `.agent/docs/session-log/2026/09/2026-09-04.commit.md`
Files: 47

```sh
git add \
  '.agent/docs/commits/09-04-2026.md' \
  '.agent/docs/decisions/ADR-008.md' \
  '.agent/docs/session-log/2026/08/2026-08-21.log.md' \
  '.agent/docs/session-log/2026/08/2026-08-30.log.md' \
  '.agent/docs/session-log/2026/09/2026-09-02.log.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.commit.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.log.md' \
  '.agent/docs/standards/directory-structure.md' \
  '.agent/docs/state.md' \
  '.agent/docs/tasks.md' \
  '.changeset/admin-topnav-view-site.md' \
  '.changeset/pre.json' \
  'README.md' \
  'apps/docs/src/content/docs/guides/quickstart.mdx' \
  'apps/docs/src/content/docs/roadmap.md' \
  'apps/test/convex/seed.ts' \
  'apps/test/src/components/component-example.tsx' \
  'apps/www/CHANGELOG.md' \
  'apps/www/README.md' \
  'apps/www/convex/seed.ts' \
  'apps/www/package.json' \
  'apps/www/src/auth/access.ts' \
  'apps/www/src/components/LogoutButton.tsx' \
  'apps/www/src/vexcms/blocks/Header/index.tsx' \
  'apps/www/vercel.json' \
  'packages/better-auth/CHANGELOG.md' \
  'packages/better-auth/package.json' \
  'packages/cli/CHANGELOG.md' \
  'packages/cli/package.json' \
  'packages/core/CHANGELOG.md' \
  'packages/core/README.md' \
  'packages/core/package.json' \
  'packages/create-vexcms/CHANGELOG.md' \
  'packages/create-vexcms/README.md' \
  'packages/create-vexcms/package.json' \
  'packages/create-vexcms/src/index.ts' \
  'packages/create-vexcms/templates/base-nextjs/package.json' \
  'packages/file-storage-convex/CHANGELOG.md' \
  'packages/file-storage-convex/package.json' \
  'packages/next/CHANGELOG.md' \
  'packages/next/package.json' \
  'packages/react/CHANGELOG.md' \
  'packages/react/package.json' \
  'packages/react/src/components/AdminLayout.tsx' \
  'packages/react/src/components/AdminSidebar.tsx' \
  'packages/richtext-plate/CHANGELOG.md' \
  'packages/richtext-plate/package.json' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-04.commit.md
```

> Two parts of this tree came from the developer, not this session:
> `apps/www/src/auth/access.ts` (adds `themes` to `resources` with `read: true`
> for the `user` role) and a `pnpm version:packages` run taking all eight
> packages to `0.1.0-alpha.11` — which accounts for every `package.json`
> version, the eight `CHANGELOG.md` files, `.changeset/pre.json`, and
> `templates/base-nextjs/package.json`'s pins. Both ride this commit per P-016.

---

### Full message

fix(www): deploy convex on build, add seed reinit and logout control

**No Vercel build had ever deployed the Convex functions.** `apps/www` carried no
`vercel.json`, no deploy script, and `build = next build`, so `usable-bee-730` kept
running whatever function code was last pushed by hand. That one gap produced both
of the failures reported from production, each disguised as something else. Running
`seed:init` executed a superseded `seed.ts`, so reseeding looked like it did nothing
and the site kept rendering old copy. Access control enforced a superseded
`access.ts` — the matrix before `9051c15` had `adminPanel: { access: false }` and no
`pages` read grant — so the freshly deployed Next.js route admitted a caller whom the
Convex queries then denied, and the panel rendered with access-denied errors in every
collection. Neither was an RBAC bug. `vercel.json` now sets
`buildCommand: "pnpm vex:deploy && pnpm build"`, with a matching `vex:deploy` script.
The build fails when `CONVEX_DEPLOY_KEY` is absent, which is deliberate: a deploy that
silently skips the backend is what caused this. `apps/www/README.md` documents the key,
the Convex-side `SITE_URL`, and both disguises.

**`seed:init` cannot update a seeded deployment, so `seed:reinit` now does.** `seed.ts`
is restructured into a single `runSeed(ctx, mode)` behind two `internalMutation` entry
points. `init` keeps its insert-only-if-absent behaviour verbatim — a fresh scaffold is
unaffected — while `reinit` patches every document it finds. Both locate documents by
natural identifier (theme or header/footer name, page slug, the `siteSettings`
singleton), never by id, so neither can duplicate a row and ids, inbound relationships,
and unseeded fields all survive. `siteSettings` needs care that the tables do not:
`upsertGlobal` replaces the whole `data` blob instead of merging field by field, so a
bare patch would have dropped `adminTheme` and every SEO field an editor had set;
existing values are carried forward beneath the seeded ones. Proven against dev — ten
documents patched, then three pages, one header, one footer and four themes, with
`adminTheme`, `metaTitle` and `twitterHandle` intact.

**The header offers no way to leave a session.** A new `LogoutButton` renders for a
real signed-in account in both the desktop bar and the mobile Sheet, with no sign-in
counterpart: the site has no public registration flow, and `AdminDemoButton` is already
the way in. An anonymous demo session deliberately does not count as signed in.
Better Auth's `anonymous()` plugin issues a genuine session for any visitor who opens
the read-only panel, so `session.user` is truthy for someone who never signed in to
anything, and offering them "Sign out" asks them to leave an account they do not have.
`session.user.isAnonymous` is the discriminator; `anonymousClient()` already types it.

**"View site" disappeared with the sidebar it lived in.** `AppSidebar`'s header is
inside a collapsible region, so the button vanished exactly when an admin wanted more
room. It moves to `AdminLayout`'s topbar, to the right of `AdminTopNav` and before
`SidebarTrigger`, and stays visible regardless of sidebar state. `ThemeToggle` stays
where it was. The topbar's existing "Home" is a breadcrumb to `basePath`, not a way out
to the site, which is why this is still needed.

**The `/features` code showcase demonstrates a collection becoming a database.** A
`testimonials` collection — the shape any marketing site has — sits beside the Convex
table `vex dev` emits from it. The collection is invented, but the output is not: it was
written into `apps/www`, registered in `vex.config.ts`, harvested verbatim from the
generator down to the `by_company` index vex infers, then reverted so no unused table
reaches production. This closes part of the `/features` gap the content spec describes.

**Both roadmaps now agree.** `apps/docs/.../roadmap.md` is authoritative and gains a
`## Future` section so it carries the same five statuses as the site's roadmap block;
www's `Analytics Adapter` and `Multi-Component Workspaces` move from `future` to
`exploring`, the bucket that already existed for open research. The React testing suite
is marked as required before the v0.1.0 launch in both. Nothing was removed from either.

**The `@latest` trap survived in six more places.** npm's `latest` tag still points at
the unrelated `0.0.20` line, so every install string must name `@alpha`. The previous
pass missed the root and `@vexcms/core` READMEs, `create-vexcms`'s README and CLI banner,
`apps/test`, and `apps/docs/.../guides/quickstart.mdx` — the page behind every call to
action on the site. Pre-release warnings were added atop the root and `create-vexcms`
READMEs, both of which npm publishes.

Also included, from the developer rather than this session: `themes` added to
`access.ts`'s `resources` with `read: true` for the `user` role, and a
`pnpm version:packages` run taking all eight packages to `0.1.0-alpha.11`.

Spec: .agent/docs/specs/2026-08-30-launch-readiness/spec.md (WP-6)
Log: .agent/docs/session-log/2026/09/2026-09-04.log.md

---

## 12:18 — fix(www): call convex deploy directly so the vercel build resolves a binary

**Committed:** `b1b4340`

Raw message: `.agent/docs/session-log/2026/09/2026-09-04.commit.2.md`
Files: 8

```sh
git add \
  '.agent/docs/commits/09-04-2026.md' \
  '.agent/docs/decisions/ADR-008.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.commit.2.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.log.md' \
  '.agent/docs/state.md' \
  'apps/www/README.md' \
  'apps/www/package.json' \
  'apps/www/vercel.json' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-04.commit.2.md
```

> Follow-up to `7200c67`, whose `buildCommand` failed on Vercel with
> `sh: vex: command not found`. **No changeset and no version bump** — only
> `apps/www` and `ADR-008` change; `@vexcms/cli` is untouched.

---

### Full message

fix(www): call convex deploy directly so the vercel build resolves a binary

The `buildCommand` shipped in `7200c67` never reached Next.js. Vercel failed on
its first line:

    > www@0.1.1-alpha.4 vex:deploy /vercel/path0/apps/www
    > vex deploy
    sh: line 1: vex: command not found

**The command exists; the binary does not.** `vex deploy` is implemented in
`packages/cli/src/commands/deploy.ts` and wired at `src/index.ts`'s
`case "deploy"`, so this is not a missing feature. `@vexcms/cli` declares
`bin: { "vex": "./dist/index.js" }`, and `dist` is gitignored — `.gitignore:24`
is `/**/dist`, and `git ls-files packages/cli/dist` returns nothing. On a fresh
CI clone that target does not exist until the CLI is built, so pnpm has no
usable shim to link into `apps/www/node_modules/.bin`. It resolves locally only
because the CLI has already been built there, which is why the script passed
review and failed in CI.

**The build now calls `convex deploy`.** `apps/www`'s `vex:deploy` script is
replaced by `deploy:convex`, and `vercel.json`'s `buildCommand` becomes
`pnpm deploy:convex && pnpm build`. `convex` is a *published* dependency of the
app (`convex@1.44.0`, `bin: { "convex": "bin/main.js" }`), so its binary is
present the moment `pnpm install` finishes and there is no build ordering to get
wrong. The alternative — building `@vexcms/cli` ahead of the app inside the
app's own build — inverts the dependency and slows every deploy to regain
something the repo already has.

**Dropping the codegen step costs nothing here.** `vex deploy` adds schema
generation and auto-migration on top of `convex deploy`, but all three generated
artifacts are committed — `convex/vex.schema.ts`, `convex/schema.ts` and
`src/vex.types.ts` — so `convex deploy` ships exactly the schema `vex deploy`
would have produced. `turbo typecheck` already fails when those artifacts drift,
which keeps schema freshness enforced at the commit rather than at deploy time.
The tradeoff is recorded: auto-migration no longer runs in CI, so a schema
change that needs it must be migrated against production by hand before the
commit that depends on it lands.

`ADR-008` is corrected rather than superseded. It was written in the same run
that introduced the broken command, so its Decision section documented something
that never executed; its Context — the stale-backend failure and the two
unrelated-looking bugs it produced — is unchanged and still accurate.

No published package is touched, so this needs no changeset and no version bump.

Spec: .agent/docs/specs/2026-08-30-launch-readiness/spec.md (WP-6)
Log: .agent/docs/session-log/2026/09/2026-09-04.log.md

---

## 12:37 — fix(www): build workspace deps before deploying, add a local vercel build check

**Committed:** `19603f6`

Raw message: `.agent/docs/session-log/2026/09/2026-09-04.commit.3.md`
Files: 11

```sh
git add \
  '.agent/docs/commits/09-04-2026.md' \
  '.agent/docs/decisions/ADR-008.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.commit.3.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.log.md' \
  '.agent/docs/standards/directory-structure.md' \
  '.agent/docs/state.md' \
  'apps/www/README.md' \
  'apps/www/package.json' \
  'apps/www/vercel.json' \
  'package.json' \
  'scripts/vercel-build-check.mjs' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-04.commit.3.md
```

> Third and final deploy fix, and the first verified before pushing:
> `pnpm check:vercel` passes from a pristine tree. **No changeset and no version
> bump** — no published package changes.

---

### Full message

fix(www): build workspace deps before deploying, add a local vercel build check

`b1b4340` got `convex deploy` running — it authenticated and reached
`usable-bee-730` — and then died in the bundler:

    ✘ [ERROR] Could not resolve "@vexcms/core/server"
        convex/theme.ts:1:26
      The module "./dist/api/server.js" was not found on the file system

**The workspace packages were never built.** A fresh clone has no
`packages/*/dist`, and every `@vexcms/*` package resolves through an `exports`
map pointing into it — `@vexcms/core`'s `./server` subpath is
`./dist/api/server.js`. This is the same fault as the previous failure, one layer
in: a gitignored build output that does not exist yet. `vercel.json`'s
`buildCommand` becomes
`pnpm turbo run build --filter=www^... && pnpm deploy:convex && pnpm build`.
`--filter=www^...` selects the app's dependencies and excludes the app itself, so
Turborepo orders them and nothing is built twice. `deploy:convex` is restored to
`apps/www/package.json`; it had been removed while troubleshooting, leaving
`vercel.json` pointing at a script that no longer existed.

Committing `dist` or adding a `postinstall` build were the alternatives. The
first puts build output in git; the second runs on every install everywhere,
including machines that do not need it.

**`next build` was going to fail next, and nothing had revealed that yet.** With
only the bundler fixed it still failed with module-not-found across
`SiteHeader.tsx`, `providers/server.tsx` and all ten block renderers. Fixing the
deploy step alone would have bought a third failed deploy.

**`scripts/vercel-build-check.mjs` (new, `pnpm check:vercel`) reproduces the
Vercel build locally.** Both failures so far were invisible in a working copy by
construction — `dist` is always populated here, so the command that fails in CI
always passes locally. The harness copies exactly what a fresh clone would have
(`git ls-files -c -o --exclude-standard`: tracked plus untracked-but-not-ignored),
which keeps uncommitted work and drops every build output, so it is usable
*before* a commit rather than after. It installs with `--frozen-lockfile` and
runs the `buildCommand` read out of `vercel.json`, so the harness cannot drift
from what Vercel runs.

The deploy segment is substituted rather than simulated. `convex deploy
--dry-run` prompts for confirmation, cannot run non-interactively, and never
reaches the bundler; the harness instead runs an esbuild bundle of `convex/**`
with Convex's own resolution conditions, which is precisely the step that broke.
The push, the deploy key and Vercel's environment are out of scope and the
harness says so when it exits.

Verified before pushing, which is the point: `pnpm check:vercel` passes from a
pristine tree — 6 dependency builds, 21 `convex/**` modules bundled with every
`@vexcms/*` import resolving, `next build` compiled. The same harness reproduces
both earlier failures when the fix is removed, so it is known to be capable of
failing rather than merely reporting success.

`ADR-008` gains the dependency-build requirement. `apps/www/README.md`'s
Deployment section is rewritten around the three build steps, why each is
load-bearing, and how to run the harness.

No published package changes, so no changeset and no version bump.

Spec: .agent/docs/specs/2026-08-30-launch-readiness/spec.md (WP-6)
Log: .agent/docs/session-log/2026/09/2026-09-04.log.md

## 21:33 — fix(core): deny caller-scoped rules in query-shaping with no user

**Committed:** (pending)

Raw message: `.agent/docs/session-log/2026/09/2026-09-04.commit.4.md`
Files: 63

```sh
git add \
  '.agent/docs/commits/09-04-2026.md' \
  '.agent/docs/harness-changelog.md' \
  '.agent/docs/product/dev-processes.md' \
  '.agent/docs/research/cdn-edge-caching-options.md' \
  '.agent/docs/research/runtime-dynamic-fields-options.md' \
  '.agent/docs/research/seo-prerendering-audit.md' \
  '.agent/docs/session-log/2026/08/2026-08-19.log.md' \
  '.agent/docs/session-log/2026/08/2026-08-21.log.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.commit.4.md' \
  '.agent/docs/session-log/2026/09/2026-09-04.log.md' \
  '.agent/docs/specs/2026-09-04-react-test-suite/spec-tasks.md' \
  '.agent/docs/specs/2026-09-04-react-test-suite/spec.md' \
  '.agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec-tasks.md' \
  '.agent/docs/specs/2026-09-04-seo-prerendering-and-lifecycle-hooks/spec.md' \
  '.agent/docs/standards/directory-structure.md' \
  '.agent/docs/standards/tooling/release-and-versioning.md' \
  '.agent/docs/state.md' \
  '.agent/docs/tasks.md' \
  '.changeset/anon-role-hardening-and-docs-cleanup.md' \
  'README.md' \
  'apps/docs/src/content/docs/fields/date.mdx' \
  'apps/docs/src/content/docs/fields/select.mdx' \
  'apps/docs/src/content/docs/guides/access-control.mdx' \
  'apps/test/src/components/providers/server.tsx' \
  'apps/www/convex/auth/options.ts' \
  'apps/www/convex/seed.ts' \
  'apps/www/package.json' \
  'apps/www/src/components/providers/server.tsx' \
  'apps/www/src/vexcms/blocks/FAQ/config.ts' \
  'packages/better-auth/src/convex/anonRole.test.ts' \
  'packages/better-auth/src/convex/anonRole.ts' \
  'packages/better-auth/src/convex/index.ts' \
  'packages/better-auth/src/index.ts' \
  'packages/core/README.md' \
  'packages/core/src/access/hasPermission.ts' \
  'packages/core/src/access/resolveAccessRule.test.ts' \
  'packages/core/src/access/resolveAccessRule.ts' \
  'packages/core/src/access/userReadSentinel.ts' \
  'packages/core/src/api/create/server.ts' \
  'packages/core/src/api/find/server.ts' \
  'packages/core/src/api/get/server.ts' \
  'packages/core/src/api/globals/find.server.ts' \
  'packages/core/src/api/globals/get.server.ts' \
  'packages/core/src/api/globals/upsert.server.ts' \
  'packages/core/src/api/remove/server.ts' \
  'packages/core/src/api/search/server.ts' \
  'packages/core/src/api/update/server.ts' \
  'packages/core/src/fields/select/config.ts' \
  'packages/core/src/fields/select/inputSchema.ts' \
  'packages/core/src/fields/select/types.ts' \
  'packages/core/src/fields/select/validator.ts' \
  'packages/core/src/media/api/mutations.ts' \
  'packages/core/src/media/api/queries.ts' \
  'packages/create-vexcms/templates/base-nextjs/convex/auth/options.ts' \
  'packages/create-vexcms/templates/base-nextjs/src/components/providers/server.tsx' \
  'packages/create-vexcms/templates/marketing-site/convex/seed.ts' \
  'packages/create-vexcms/templates/marketing-site/src/vexcms/blocks/FAQ/config.ts' \
  'packages/next/README.md' \
  'packages/react/README.md' \
  'packages/react/src/components/views/GlobalEditView.tsx' \
  'packages/richtext-plate/README.md' \
  'scripts/sync-template-versions.mjs' \
  'turbo.json' \
  && git commit -F .agent/docs/session-log/2026/09/2026-09-04.commit.4.md
```

> Fourth run of the day, unrelated to the deploy fixes above: closes a
> query-shaping gap in `@vexcms/core`'s access-control evaluator, adds
> `@vexcms/better-auth`'s `anonRoleDatabaseHook`, wires both into `apps/www`
> and the `base-nextjs` template, and folds every other outstanding change in
> the working tree (docs cleanup, `GlobalEditView` accent, provider
> double-mount fix, release-tooling) into this one commit per P-016. Changeset
> `anon-role-hardening-and-docs-cleanup.md` — next `changeset version` run
> lands `0.1.0-alpha.12`.

---

### Full message

fix(core): deny caller-scoped rules in query-shaping with no user

**`resolveAccessRule`'s query-shaping pass now shares `hasPermission`'s sentinel
guard.** The earlier `anon-constraint-fail-closed` fix made `hasPermission`'s
per-document pass deny a caller-scoped rule reached through `anonRole` when
there is no user, instead of crashing or silently widening.
`resolveAccessIndex`/`resolveAccessConstraint` — the sibling pass deciding
which `withIndex`/`.filter()` a query gets — still defaulted the caller to a
plain `{}`, so a rule like `base-nextjs`'s own `fq.eq("email", user.email)`
compiled `eq("email", undefined)` for a sessionless caller: Convex reads
`undefined` as "field is absent," so `find` read a widened range before the
per-document check (correctly) discarded the rows. The sentinel-`Proxy`
technique is extracted into a shared `createUserReadSentinel`
(`packages/core/src/access/userReadSentinel.ts`), used by both
`hasPermission.ts`'s `resolveConstrainedCheck` and `resolveAccessRule.ts`'s
`classifyRole`, so the two passes can no longer disagree about which rows a
rule describes. Two new tests in `resolveAccessRule.test.ts` pin it.

**That guard was unreachable from every real server call.** `create`, `get`,
`update`, `remove`, both `globals` write paths, both `media` API files, and
`find`/`search`'s own per-document filters all normalized
`user: args.auth?.user ?? {}` before calling `hasPermission` — collapsing a
sessionless caller's `null` to `{}` upstream defeats a `null`-keyed sentinel
before it runs. All 19 call sites across `packages/core/src/api/**` and
`packages/core/src/media/api/**` now pass `?? null` through, matching what
`find`/`search` already did for their index-resolution calls.

**`@vexcms/better-auth` gains `anonRoleDatabaseHook(role)`.** Better Auth's
`additionalFields` default-fills `roles` on every new user regardless of
`isAnonymous`, so an anonymous-plugin visitor silently lands in whatever role
every other signup gets and `anonRole` never actually applies to it. The new
`databaseHooks.user.create.before` hook stamps the same role string passed to
`defineAccess({ anonRole })` onto anonymous-plugin users specifically, keeping
the two in lockstep by construction. Wired into `apps/www` and the
`base-nextjs` template's `auth/options.ts`, both still assigning anonymous
visitors the existing `"user"` role — no permission-matrix change.

**Docs cleanup.** Example collections no longer hand-roll a
`status`/`publishedAt` lifecycle pair (`apps/www/convex/seed.ts`, the
`marketing-site` template's `seed.ts`, the root and `@vexcms/core` READMEs,
the `select`/`date` field doc pages), and the access-control guide's
constraint examples narrow on a `visibility` field instead.
`@vexcms/core`'s `select` field JSDoc example moved off the same
`status: draft/published` shape to `tier: free/pro`. `@vexcms/next`,
`@vexcms/react`, and `@vexcms/richtext-plate` READMEs now name the real
package instead of a pre-rename one; unshipped feature sections (versioning &
drafts, live preview, impersonation) are now roadmap notes; documented
exports that were never exported are removed from every table and example;
`apps/www`'s FAQ block copy dropped its "draft/publish workflows, live
preview" claim. `GlobalEditView`'s heading now wraps the global's label in
`text-primary`, matching how the rest of the admin panel distinguishes a
document's identity from its chrome.

**`providers/server.tsx`'s double `ConvexClientProvider` mount removed**
across `apps/www`, `apps/test`, and the `base-nextjs` template.
`ClientProviders` already renders `ConvexClientProvider` and is nested inside
`ServerProviders`, so the outer copy built a second
`ConvexReactClient`/`QueryClient` pair on every server render with no
consumer. `marketing-site` has no own copy of this file, so nothing there
needed the fix.

**Release tooling: `apps/www` now rides the release train.**
`scripts/sync-template-versions.mjs` sets `apps/www/package.json#version` to
the core package version after `changeset version`, since `www` is
changesets-ignored and consumes the packages via `workspace:*` — its manifest
had drifted to `0.1.1-alpha.4` against a `0.1.0-alpha.11` release line. The
guard on the `www` path is warn-and-skip, not fatal like the templates guard,
since `www` is private and its version is cosmetic.

**Three outstanding changesets folded into one**
(`.changeset/anon-role-hardening-and-docs-cleanup.md`), covering every
package this run touches (`@vexcms/core`, `@vexcms/react`, `@vexcms/next`,
`@vexcms/richtext-plate`, `@vexcms/better-auth`, `create-vexcms`, `www`) at
patch, landing the whole fixed group on `0.1.0-alpha.12` at the next
`changeset version` run.

Also included: two new draft specs and three research docs, planning
artifacts from other sessions with no code dependency yet.

Spec: .agent/docs/specs/2026-08-12-rbac-access-control/spec.md · .agent/docs/specs/2026-08-23-access-index-resolution/spec.md
Log: .agent/docs/session-log/2026/09/2026-09-04.log.md
