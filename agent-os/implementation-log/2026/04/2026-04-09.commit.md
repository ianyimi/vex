revert(react): revert data table implementation due to layout issues

Attempted full implementation of spec 008 (data table component) with TanStack Table,
server-side pagination via Convex listPaginated, URL state management with nuqs, and
localStorage column preferences. Implementation was technically functional but had
severe layout/styling issues — table did not take up full container width and visual
appearance was not production-ready.

Reverted all changes to defer data table to future iteration when styling approach
can be properly prototyped and validated before full feature implementation.

Files removed:
- packages/react/src/components/data-table/* (all components and hooks)
- apps/www/convex/vex/model/collections.ts (listPaginated model layer)
- Convex listPaginated query and vexConvexApi references

CollectionListView restored to placeholder state with simple document list.
Spec 008 remains uncompleted — all checkboxes unchecked.

Spec: agent-os/product/008-data-table-component.md (postponed)
Log: agent-os/implementation-log/2026/04/2026-04-09.ideaLog.md
