// Stubbed — block components removed in rebuild (@vexcms/ui not yet rebuilt)
export const blockComponents: Record<string, never> = {}

// Re-export configs for convenience in client code
export { allBlocks, footerBlocks, headerBlocks, pageBlocks } from "./config"
