## Commit — 2026-04-22 00:39

chore(repo): bootstrap Pi agent harness in .pi/

Migrate the existing agent-os/, memory/, and .claude/ context into a new
.pi/agent-docs/ tree that becomes the single source of truth for agent
sessions. Product docs (mission, roadmap), standards (global/backend/
frontend/testing, adding-a-field-type, developer-preferences, JSDoc
memory notes), legacy numbered specs, and the implementation log all
move over. Originals are left on disk and marked deprecated in AGENTS.md
so downstream references keep working until verified safe to delete.

Scaffold five project-local prompts under .pi/prompts/: 1-dev-spec
(monorepo-aware, pre-seeded with the six developer preferences already
encoded in agent-os), 2-sync-spec (typecheck + vitest + /document +
changeset upkeep per affected workspace), 3-commit (conventional commits
with required package-name scope and a changeset prompt on published-
package changes), document (JSDoc with the Input-vs-resolved rule from
memory/), and a bespoke changeset prompt that drafts Changesets release
notes from commits since the last .changeset/*.md entry.

Configure high-care workflow tier so the agent writes specs and reviews
but does not edit packages/*/src/** unilaterally. Verification targets
apps/www at http://localhost:3020 via the built-in browse tool plus
pnpm --filter <pkg> typecheck && test and tmux pane 1. Debug hierarchy
is UI-first to match where most admin bugs surface.
