## Commit — 2026-07-10 18:51

chore(deps): upgrade to TypeScript 6.0 and update dependencies

Upgraded TypeScript from 5.9.3 to 6.0.3 across the monorepo and updated the pnpm lockfile with latest dependency versions including React 19.2.7, Prettier 3.9.5, and other ecosystem packages.

Fixed SSR cache leak in Convex provider by creating separate client instances for server and browser environments. Previously, module-level singletons could share state across SSR requests. Now server-side renders get fresh instances per request while the browser maintains singleton behavior for WebSocket reactivity.

Added media collections to admin nav breadcrumb logic so media routes display proper navigation context. Fixed upload field component missing FormLabel in read-only and edit modes. Improved collection edit view with form state subscription to conditionally enable/disable save and cancel buttons based on whether values have changed, and reset form state on successful save.

Added TypeScript 6.0 deprecation handling in create-vexcms package and updated tsconfig extends paths for better-auth and cli packages.
