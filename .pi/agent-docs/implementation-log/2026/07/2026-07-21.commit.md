## Commit — 2026-07-21 03:08

feat(pagination): add totalDocs counting and refactor query builders

- Added `totalDocs?: boolean` option to PaginationOptions to support total document counting on first page only
- Refactored `find` and `search` server functions to extract query building into reusable `buildQuery()` helpers, reducing duplication
- Implemented totalDocs counting logic that safely handles Convex transaction limits (returns `null` if >32k docs)
- Extended PaginationResult type with optional `totalDocs: number | null` field
- Completely rewrote DataTable component with row selection checkboxes, bulk action support, and load-more pagination
- Added `usePaginatedQuery` hook for managing cursor-based pagination state on client
- Updated Collection table config with new `serverPageSize` property (default 100) for initial server fetch
- Updated NextAdminPage to use new pagination API with totalDocs counting on first page

Spec: .pi/agent-docs/specs/34-pagination-bulk-actions.md
Log: .pi/agent-docs/implementation-log/2026/07/2026-07-21.ideaLog.md
