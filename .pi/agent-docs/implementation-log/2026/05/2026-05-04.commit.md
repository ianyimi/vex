## Commit — 2026-05-04 00:16

chore: document tmux workspace map and fix Convex skip in edit view

Replace vague "tmux pane 1" guidance with concrete `project-vex:0.0`
(Next.js + tsup) and `project-vex:0.1` (Convex + vex CLI) targets across
the harness so future agents can capture the right pane without guessing.
Adds a new `tmux-workspace.md` product doc with full pane map and capture
commands, updates `debug-hierarchy.md` to split Next.js vs Convex error
sources, and extends `.pi/AGENTS.md` and `dev-processes.md` with a Tmux
Workspace section plus a Dotfiles & Base Harness reference (chezmoi
paths, `cma` alias, `pi-agent-base/` source of truth).

Fix `CollectionEditView` to use Convex's `"skip"` sentinel instead of
passing a placeholder `id: ""` with `enabled: isEditing` — the previous
shape relied on TanStack Query's `enabled` flag while still constructing
an invalid Convex query key, which is fragile under HMR. Also drops a
redundant `cn(className)` wrapper in `Icon` since there is nothing to
merge.

---

## Commit — 2026-05-04 03:57

chore(react): translate Claude Design to Stark×Ember token system

The old `globals.css` held a minimal placeholder Stark×Ember palette.
This replaces it with a complete, semantically deliberate translation
from the Claude Design artboard (`.pi/design/claude-design/admin.css`).
The key semantic change: `--primary` is now ember (#E8622A) rather than
graphite — the admin UI CTA colour is now ember across all components.
`--accent` becomes ember tint; extended border variants (`--border-strong`,
`--border-soft`) and muted-foreground scales (`-subtle`, `-faint`) are
added; a five-step shadow system (xs → pop) is introduced; dark-mode
values are recalibrated for contrast. Tailwind `@source` scan directives
are added so classes resolve even when a tsup watcher dies.

Component updates follow the new tokens: `Button` moves to `rounded-sm`,
`h-8` default height, and `text-[13px]`; `VexLink` gains `className`
forwarding and `transition-colors`; `AdminTopNav` breadcrumbs highlight
the active segment in `text-primary` (ember) with `hover:text-primary-hover`
on inactive links.

Spec 22 gains Decisions 11–14 (custom preview component API,
`useRelationshipPickerOptions` hook, `Input` loading prop, generic
`RelationshipPreviewProps`) plus full implementation blocks for Steps 7
and 8, a Master Port Inventory table mapping master-branch code to the
rebuild approach, and updated acceptance tests. The spec is now
execution-ready.

Spec: .pi/agent-docs/specs/22-relationship-field.md
