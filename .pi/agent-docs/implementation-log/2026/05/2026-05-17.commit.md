## Commit — 2026-05-17 05:03

feat(core,better-auth,react,docs): upgrade to Convex 1.39.1 and improve type signatures and documentation

**Convex upgrade**: `^1.31.5` → `^1.39.1` across the workspace. Moved `convex` from catalog to `overrides` in pnpm-workspace.yaml to ensure consistent version across all packages. Updated `@convex-dev/better-auth`, `@convex-dev/react-query`, `convex-helpers`, `convex-test` peer dependencies to match.

**Type parameter renaming** (continuing from previous commit): `TMeta` → `TFieldMeta` and `TSlug` → `TCollectionSlug` across all field types, framework types, and React components. This aligns with the naming already established on `CollectionConfig` (`TFieldMeta`/`TCollectionMeta`), making the type system consistent throughout the public API.

**Zod type alignment**: `ZodSchema` → `ZodType` in all field input schemas. `ZodType` is the correct Zod 4 export.

**Admin defaults cleanup**: Removed `description: ""` from admin defaults in all field config functions — it was explicitly setting an empty string where `undefined` is the correct no-op state.

**Bug fix**: `FormDescription.tsx` was reading `field.admin.description` (wrong path) — fixed to `field.description`.

**Framework type updates**: Added `TFieldMeta`/`TCollectionMeta` generics to `CollectionListViewProps` and `CollectionEditViewProps` to match the updated `CollectionConfig` signature.

**New `@vexcms/better-auth/convex` package**: Complete Convex DB adapter for Better Auth — wraps Better Auth's DB interface operations as proper Convex internal mutations/queries. Follows the same factory pattern as `queryApi`/`mutationApi` in `@vexcms/core/convex`. Includes `convex/adapter.ts` (DB operations), `convex/db.ts` (CRUD handler implementations), `convex/types.ts` (handler signatures), `convex/utils.ts` (query building, pagination, unique field checks), and `extract/` for auth table introspection.

**JSDoc additions**: Added missing JSDoc to `packages/better-auth/src/index.ts` (package-level overview with examples for `betterAuthAdapter` and `authDbApi` wiring), `packages/react/src/components/form/FormDescription.tsx` (component with `@param` docs), `apps/www/convex/auth/db.ts` (authDbApi exports with Convex schema wiring example and `@see` reference to the factory).

**Updated document prompt**: Changed default from "ask user what to document" to `git status --porcelain` — the command now always documents uncommitted files and never asks. Updated Usage section and Step 1 accordingly.