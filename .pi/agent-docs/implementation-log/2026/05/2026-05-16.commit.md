## Commit — 2026-05-16 22:18

refactor(core,react,next): standardize generic type parameter names and clean up admin defaults

Renames `TMeta` → `TFieldMeta` and `TSlug` → `TCollectionSlug` across all field types, framework types, and React components. This aligns with the naming convention already established on `CollectionConfig` (`TFieldMeta`/`TCollectionMeta`), making the type system consistent throughout the public API.

Also updates `ZodSchema` → `ZodType` in all field input schemas — `ZodType` is the correct Zod 4 export. Removes the redundant `description: ""` from admin defaults in all field config functions — it was explicitly setting an empty string where undefined is the correct no-op state.

Bug fix: `FormDescription.tsx` was accessing `field.admin.description` (wrong path) instead of `field.description`.

Adds missing `TFieldMeta`/`TCollectionMeta` generics to `CollectionListViewProps` and `CollectionEditViewProps` to match the updated `CollectionConfig` signature. Minor Next.js adapter cleanup removes an unnecessary `CollectionConfig` import and type cast.