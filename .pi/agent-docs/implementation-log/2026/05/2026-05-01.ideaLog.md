## Sync Spec — 20:02

Spec: `.pi/agent-docs/specs/2026-05-01-design-tokens.md`
Packages touched: `@vexcms/react` (styles.css), `apps/www` (globals.css, layout.tsx)

Patterns extracted:
- Package CSS defaults in `@layer base` → `developer-preferences.md` + `1-dev-spec.md`

Patterns rejected:
- none

Upkeep tasks:
- typecheck: `@vexcms/react` fail (pre-existing relationship field errors, unrelated to spec) · `www` fail (same root cause)
- tests: `@vexcms/core` 210/210 pass · `@vexcms/react` no tests
- document: skipped (CSS-only spec, no exported TypeScript APIs changed)
- changeset: skipped (pre-launch alpha)

Notes:
- Typecheck errors in `@vexcms/react` are pre-existing: `relationship` component not yet registered in `FieldComponentMap`. Confirmed by stashing spec changes and seeing different-but-also-failing errors before them. Unblocked by spec 1i (relationship picker).
- `Inter` font removed from layout; `Geist` is now `--font-sans` app-wide.
- `--radius` changed from `0.5rem` to `4px`; radius scale in `@theme inline` updated to pinned pixel values (`--radius-sm: 2px`, `--radius-md: 4px`) to avoid broken `calc()` output.
