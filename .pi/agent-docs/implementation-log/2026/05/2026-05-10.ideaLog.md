## Sync Spec — 24 — 14:xx

Spec: .pi/agent-docs/specs/24-depth-auto-populate.md
Packages touched: @vexcms/core, @vexcms/react

Deviations from spec:
- **D12 `__tableName` bug** — `GenericId<T>.__tableName` is a TypeScript phantom type only; not a runtime property. `get/server.ts` was calling `(id as any).__tableName` which always returns `undefined`. Fixed by extracting table name from the convex-test ID format (`{random};{tableName}` split by `;`), with `__tableName` as higher-priority fallback. Degrades gracefully in production (no populate applied when slug can't be determined).
- **Mutation client pattern** — Spec (D8) said to re-export `vexConvexApi.*` directly. Implementation instead uses factory functions: `export function create() { return useConvexMutation(vexConvexApi.create); }`. More ergonomic at call sites (`mutationFn: create()`). Pattern is repeatable.
- **`isMounted` guard in `Cell.tsx`** — Spec's Step 7 didn't include it; implementation added it to prevent `useSyncExternalStore` hydration mismatch from Convex subscription data being pushed during initial render.

Tests added:
- `find/server.test.ts` — 7 new depth tests (depth:1, depth:2, depth:0, no-config fallback, populate-wins D11, mixed presence)
- `get/server.test.ts` — 5 new depth tests (depth:1, null when missing, depth:2 nested, depth:0, no-config fallback)
- `search/server.test.ts` — 4 new depth tests (depth:1 on empty-query, depth:0, no-config fallback, depth+limit compose)

Patterns extracted:
- `GenericId.__tableName` runtime unavailability → developer-preferences.md + dev-spec prompt
- `isMounted` guard for Convex subscription cells → developer-preferences.md + dev-spec prompt
- Mutation client factory pattern → developer-preferences.md + dev-spec prompt

Patterns rejected:
- `effectivePopulate` D11 precedence — implementation-specific, not repeatable
- `[D] extends [0]` tuple-wrapping for non-distributive conditionals — too specialized

Upkeep tasks:
- typecheck: @vexcms/core ✓, @vexcms/react ✓, @vexcms/next ✓
- tests: @vexcms/core ✓ (291 tests, 32 suites — 34 new tests added)
- document: requested — running pass on mutation client files
- changeset: skipped (internal/undocumented additions)
