/**
 * Internal collection definitions used to power built-in VexCMS features.
 *
 * This module is not part of the public API — it is re-exported only for
 * consumption inside `@vexcms/core` itself.
 */
export const internalCollections = [];
