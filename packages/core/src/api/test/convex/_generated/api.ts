// Stub _generated/api.ts for convex-test module root detection.
// convex-test requires at least one path containing "_generated" in the
// modules map passed to convexTest() to locate the module root. Since these
// tests call library functions directly via t.run() and don't use any
// registered Convex functions, this file is never actually imported at runtime.
export {};
